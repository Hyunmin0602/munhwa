# 통합 화면 디자인·반응형 설계서

## 1. 재검토 결론

통합 화면은 모든 사업의 데이터를 나열하는 대시보드가 아니라, 사용자가 **지금 처리할 일**을 우선순위로 판단하고 원래 업무 화면으로 빠르게 이동하는 작업 허브여야 한다. 따라서 카드 수를 늘리기보다 날짜, 마감일, 사업 식별을 먼저 보여 준다.

기존 화면과의 일관성을 위해 `indigo`, `emerald`, `amber`, `rose`, `gray` 팔레트와 Lucide 아이콘을 유지한다. 카드, 필터, 내비게이션은 현재 디자인 가이드를 따르되, 통합 화면에서는 반복 카드의 모서리를 `rounded-lg` 또는 `rounded-xl`로 제한한다. 한 페이지의 모든 영역을 둥근 카드로 감싸지 않는다.

### 현재 반응형 구조에서 확인한 개선점

| 항목 | 현재 상태 | 설계 결정 |
| --- | --- | --- |
| 모바일 하단 탭 | 홈·회의록·칸반·일정·아카이브 5개 | 통합을 추가해 6개로 만들지 않는다. 전역 화면은 `더보기` 시트에서 제공한다. |
| 모바일 사이드바 | 렌더링 가능하지만 헤더에서 여는 제어가 없음 | 상단 헤더에 메뉴 아이콘을 두고 `Sidebar.open` 상태를 연결한다. |
| 하단 안전 영역 | 프로젝트 선택 막대가 있을 때 본문 여백과 충돌 가능 | 페이지별 `--bottom-nav-height` CSS 변수 또는 프로젝트 화면의 추가 bottom padding을 사용한다. |
| 태블릿 | `md`부터 데스크톱 사이드바가 고정되어 콘텐츠 폭을 많이 차지함 | 768–1023px에서는 축소 레일 네비게이션 또는 오버레이 사이드바를 사용한다. |
| 필터 | 다중 선택 시 가로 폭이 커짐 | 모바일은 필터 시트, 태블릿 이상은 인라인 필터 바를 사용한다. |

## 2. 화면 원칙

1. **시간 우선**: 오늘, 임박, 최신 순서가 사업별 구획보다 앞선다.
2. **사업 맥락 유지**: 모든 항목에 사업 색상 점과 사업명을 표시한다.
3. **한 항목 한 이동**: 통합 목록에서는 편집하지 않고, 탭하면 해당 사업의 원래 상세 화면으로 이동한다.
4. **밀도 조절**: 작은 화면에서 설명·보조 배지를 숨기되 제목, 사업명, 날짜, 상태는 남긴다.
5. **터치 우선**: 버튼과 행의 최소 터치 영역은 $44\times44\text{ px}$로 한다.
6. **필터의 가시성**: 현재 적용된 필터 수와 초기화 동작은 항상 확인 가능해야 한다.

## 3. 정보 구조

### 3-1. 공통 상단

```text
통합 화면                         [필터 아이콘]
오늘 처리할 업무를 사업별로 모았습니다.     [새로고침]

[전체] [칸반] [일정] [문서] [회의록]
```

- 제목은 `text-xl md:text-2xl font-bold`로 고정한다. 뷰포트 폭에 따라 글자 크기를 계산하지 않는다.
- 필터는 익숙한 `SlidersHorizontal` 아이콘 버튼을 사용하고 `aria-label="필터 열기"`를 지정한다.
- 새로고침은 `RotateCw` 아이콘 버튼으로 처리하며 텍스트 버튼을 만들지 않는다.
- 유형 탭은 가로 스크롤 가능한 세그먼트 컨트롤이다. 스크롤바를 숨기지 않고, 선택된 탭이 자동으로 가시 영역에 남도록 한다.

### 3-2. 요약 띠

상단 탭 아래에는 카드 묶음 대신 높이가 고정된 요약 띠를 둔다.

| 항목 | 표시 조건 | 색상 |
| --- | --- | --- |
| 오늘 일정 | 오늘 시작하거나 진행 중인 일정 수 | `indigo` |
| 마감 임박 | 7일 내 마감, 미완료 작업 수 | `amber` |
| 새 문서 | 7일 내 수정 문서·회의록 수 | `emerald` |

- 데스크톱: 3개 열
- 태블릿: 3개 열, 더 작은 패딩
- 모바일: 가로 스크롤 스냅 항목 3개. 각 항목은 화면 폭의 72%로 고정해 내용이 줄어도 크기가 변하지 않게 한다.

### 3-3. 타임라인 섹션

기본 탭은 다음 순서를 사용한다.

1. `오늘과 임박한 일정`
2. `마감이 가까운 작업`
3. `최근 문서`

섹션은 흰색 카드로 감싸지 않는다. 섹션 제목과 우측의 `전체 보기` 링크 아래에 밀도 높은 행 목록을 둔다.

공통 행 구조:

```text
[유형 아이콘] 제목                              [상태 배지]
             사업 색상점 사업명 · 날짜 · 보조 정보       >
```

- 행 높이: 모바일 72px, 태블릿/데스크톱 68px 이상
- 행 사이는 `border-b border-gray-100`으로 구분한다.
- 우측 화살표는 `ChevronRight` 아이콘만 사용한다.
- 제목은 1줄 말줄임, 보조 정보는 1줄 말줄임으로 제한한다.
- 고우선 작업만 `rose` 점 또는 배지로 강조한다. 모든 항목에 강한 색을 사용하지 않는다.

## 4. 브레이크포인트별 설계

### 4-1. 모바일: 360–767px

#### 레이아웃

- 상단 헤더: 56px 고정, 메뉴 버튼·페이지명·필터 버튼을 한 줄에 배치한다.
- 본문: `px-4 py-4`, 콘텐츠 최대 폭 제한 없음.
- 하단 네비게이션: 4개 고정 항목 `홈`, `통합`, `사업`, `더보기`.
- `사업`은 현재 사업이 있으면 해당 사업의 기본 칸반으로, 없으면 사업 선택 시트를 연다.
- `더보기` 시트에 `회의록`, `아카이브`, `일정`, 프로젝트 전환, 계정 메뉴를 둔다.

현재 5개 탭 구조를 이 4개 전역 탭 구조로 교체한다. 사업별 칸반·일정·아카이브를 하단에 모두 고정하면 통합 및 전역 메뉴가 들어갈 자리가 없고, 레이블의 최소 폭도 확보할 수 없다.

#### 필터

- 필터 아이콘을 누르면 화면 하단에서 필터 시트가 열린다.
- 시트는 사업 다중 선택, 기간, 사업 상태, 정렬만 제공한다.
- 유형 선택은 상단 탭으로 이미 제공하므로 시트에서 중복하지 않는다.
- `적용 n개` 버튼은 하단 안전 영역 위에 고정하고, `초기화`는 텍스트 링크로 둔다.
- 사업 선택은 체크박스 목록을 사용한다. 색상 점, 사업명, 선택 수를 함께 표시한다.

#### 목록 밀도

- 작업 담당자 이름과 우선순위는 첫 줄에 들어가지 않을 때 숨기고, 날짜를 우선 보존한다.
- 문서 공개 범위는 아이콘+짧은 라벨 `비공개`/`내부`/`외부`로만 표시한다.
- 한 화면에 요약 띠 1개와 첫 섹션의 3–4개 행이 보이도록 한다.

#### 상태

- 초기 로딩: 행 높이를 고정한 스켈레톤 5개를 사용한다.
- 빈 상태: 해당 필터에서 결과가 없다는 문구와 필터 초기화 버튼을 제공한다.
- 오류: 재시도 아이콘 버튼과 짧은 오류 문구를 한 행으로 표시한다.

### 4-2. 태블릿: 768–1023px

#### 레이아웃

- 왼쪽 내비게이션은 기본 64px 아이콘 레일로 표시한다.
- 레일의 사업 아이콘을 누르면 280px 너비의 오버레이 사업 패널이 열린다.
- 1024px 이상에서만 현재의 256px 전체 사이드바를 상시 표시한다.
- 메인 콘텐츠는 `max-w-6xl mx-auto`, `px-6 py-6`을 사용한다.

#### 필터 및 목록

- 상단의 유형 탭과 기간 칩은 한 줄에 둔다.
- 사업 필터는 `Filter` 아이콘 버튼이 여는 팝오버로 제공한다.
- 요약 띠는 3열을 유지한다.
- 기본 타임라인은 2열 그리드가 아니라 단일 열로 유지한다. 일정·작업·문서의 시간 흐름을 분리하지 않는다.
- 각 행은 보조 정보(담당자, 문서 작성자)를 표시한다.

#### 가로 모드

- 768px 이상 가로 화면에서 필터 바가 넘치면 두 줄로 줄바꿈한다.
- 필터 버튼과 새로고침 버튼은 고정 폭 40px을 유지한다.
- 필터 라벨의 긴 사업명은 말줄임 처리하고 선택 수를 우선 표시한다.

### 4-3. 데스크톱: 1024px 이상

- 전체 사이드바 256px을 유지한다.
- 메인 콘텐츠는 `max-w-6xl`로 제한하고, 가장 넓은 화면에서도 행이 과도하게 길어지지 않게 한다.
- 상단에는 유형 탭, 기간 칩, 사업 다중 선택, 정렬을 인라인으로 둔다.
- 기본 탭은 `lg`에서만 2열 보조 배치를 허용한다.
  - 왼쪽: 오늘/임박 일정, 마감 임박 작업
  - 오른쪽: 최근 문서/회의록
- 단, 각 열의 카드 안에 또 다른 카드를 넣지 않는다. 섹션은 배경 없는 세로 목록을 유지한다.

## 5. 인터랙션 설계

| 동작 | 모바일 | 태블릿·데스크톱 |
| --- | --- | --- |
| 유형 변경 | 상단 탭 가로 스크롤 | 인라인 탭 |
| 사업 필터 | 하단 시트 | 팝오버 |
| 기간 선택 | 하단 시트 | 기간 칩 |
| 항목 열기 | 행 전체 탭 | 행 hover + Enter |
| 더 불러오기 | 하단 `더 불러오기` 버튼 | 자동 페이지 추가 대신 명시적 버튼 |
| 새로고침 | 아이콘 버튼 | 아이콘 버튼 |

- 필터 적용, 탭 변경, 더 불러오기는 URL 검색 파라미터를 즉시 갱신한다.
- 데이터 재조회 중 기존 목록은 유지하고, 상단에 얇은 진행 표시만 보인다.
- 포커스는 필터 시트가 열리면 첫 번째 제어로 이동하고, 닫히면 필터 버튼으로 복귀한다.
- 프로젝트 전환 시트와 필터 시트는 동시에 열지 않는다.

## 6. 컴포넌트 분리

```text
src/app/dashboard/integrated/page.tsx
src/components/integrated/IntegratedHeader.tsx
src/components/integrated/IntegratedSummaryRail.tsx
src/components/integrated/IntegratedFilters.tsx
src/components/integrated/IntegratedFilterSheet.tsx
src/components/integrated/IntegratedTimeline.tsx
src/components/integrated/IntegratedItemRow.tsx
src/components/navigation/MobileMoreSheet.tsx
```

- `IntegratedItemRow`는 유형별 텍스트를 조건부로 바꾸되 레이아웃 구조는 공유한다.
- 필터 상태는 URL 파라미터에서 파생한다. 전역 Zustand 상태로 복제하지 않는다.
- 하단 네비게이션은 통합 화면 전용 예외를 만들지 않고 4개 전역 탭 구조로 재설계한다.

## 7. 접근성과 시각 검증

### 접근성

- 모든 아이콘 버튼에 `aria-label`과 tooltip을 둔다.
- 선택 탭은 `aria-selected`, 탭 패널은 `role="tabpanel"`을 사용한다.
- 색상만으로 상태를 구분하지 않는다. 우선순위·공개 범위에 텍스트 또는 아이콘을 함께 쓴다.
- 키보드로 탭, 필터 팝오버, 시트, 목록 이동이 가능해야 한다.

### 시각 검증 뷰포트

| 뷰포트 | 확인 항목 |
| --- | --- |
| 360 x 800 | 하단 탭 4개 레이블, 필터 시트, 안전 영역, 긴 사업명 |
| 390 x 844 | 요약 띠 스냅, 목록 말줄임, 회의록/문서 배지 |
| 768 x 1024 | 아이콘 레일, 필터 팝오버, 3열 요약 띠 |
| 1024 x 768 | 전체 사이드바 전환, 인라인 필터 줄바꿈 |
| 1440 x 900 | 최대 콘텐츠 폭, 2열 섹션, 빈 여백 균형 |

Playwright 화면 캡처에서 텍스트 겹침, 잘린 터치 제어, 하단바에 가려진 마지막 항목, 빈 캔버스를 확인한다.

## 8. 구현 우선순위

1. 모바일 헤더 메뉴 제어와 4개 전역 하단 탭 구조를 먼저 정비한다.
2. 통합 API와 공통 `IntegratedItemRow`를 구현한다.
3. 모바일 필터 시트와 URL 상태 동기화를 구현한다.
4. 태블릿 아이콘 레일 및 필터 팝오버를 추가한다.
5. 데스크톱의 인라인 필터와 2열 보조 배치를 추가한다.
6. 실제 데이터가 있는 계정으로 5개 기준 뷰포트를 캡처해 시각 회귀를 확인한다.