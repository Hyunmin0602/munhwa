# 통합 화면 재검토 오류 수정 계획

## 1. 목표

통합 화면 재검토에서 확인된 데이터 기간 오류, 모바일 진입점 누락, 오류 상태 부재, 필터 동작 불일치, API 계약 불일치, 대량 데이터 조회 문제를 해결한다.

수정 원칙은 다음과 같다.

- 권한과 데이터 정확성을 UI 개선보다 먼저 수정한다.
- 기존 사업별 상세 화면과 아카이브 공개 범위 규칙은 변경하지 않는다.
- URL, UI 상태, API 파라미터의 계약을 하나로 맞춘다.
- 대량 데이터 대응은 기능 회귀 없이 단계적으로 전환한다.

## 2. 우선순위

| 우선순위 | 항목 | 위험 | 완료 목표 |
| --- | --- | --- | --- |
| P0 | 기간 필터가 과거 데이터를 포함 | 잘못된 업무 판단 | 범위별 시작·종료 시각을 서버에서 모두 제한 |
| P0 | 모바일 통합 탭 누락 | 핵심 화면 접근 불가 | `홈·통합·사업·더보기` 4개 탭 구성 |
| P1 | API 오류가 빈 상태로 보임 | 장애를 데이터 없음으로 오인 | 오류 상태와 재시도 추가 |
| P1 | 필터 `적용` 버튼과 실제 동작 불일치 | 잘못된 사용자 기대 | 임시 선택 후 적용 또는 버튼 명칭 정합화 |
| P1 | `types` 다중 값 계약 불일치 | 공유 URL 동작 불일치 | API/URL/UI 중 단일 유형 규칙으로 통일 |
| P2 | 전체 데이터를 읽은 후 메모리 페이지네이션 | 데이터 증가 시 성능 저하 | 유형별 제한 조회와 안정 커서 도입 |

## 3. P0: 기간 필터 정확성

### 문제

현재 API는 종료 시각만 사용한다. 예를 들어 `today` 요청이 오늘 자정 이전의 모든 일정과 과거 마감 작업을 포함한다.

### 구현

`src/app/api/dashboard/integrated/route.ts`에 `getRangeWindow`를 추가한다.

```ts
type DateWindow = { start: Date; end: Date | null };
```

| range | start | end |
| --- | --- | --- |
| `today` | 서버 시간 기준 당일 00:00:00 | 당일 23:59:59.999 |
| `7d` | 현재 시각 | 현재 시각 + 7일 |
| `30d` | 현재 시각 | 현재 시각 + 30일 |
| `all` | 없음 | 없음 |

각 조회에 같은 윈도우를 적용한다.

- 작업: `dueDate: { gte: start, lte: end }`; `all`은 기존 전체 조회
- 일정: `startDate: { gte: start, lte: end }`
- 문서/회의록: `updatedAt: { gte: start, lte: end }`

작업의 마감일이 없는 항목은 `today`, `7d`, `30d`에서 제외한다. 이는 “마감 임박 작업”이라는 통합 화면 의미와 일치한다. `all`에서는 포함하되 날짜가 없음을 UI에 명확히 표시한다.

### 검증

- 어제 일정·마감 작업이 `today`에 없다.
- 오늘 23:59:59 일정은 `today`에 있다.
- 7일 후 경계와 30일 후 경계의 포함 여부가 일관된다.
- `all`만 과거 데이터와 기한 없는 작업을 포함한다.

## 4. P0: 모바일 통합 진입점

### 문제

현재 하단 탭은 `홈·사업·더보기` 세 개이며 설계한 `통합` 탭이 없다.

### 구현

`src/components/BottomTabBar.tsx`를 다음 네 개 전역 탭으로 수정한다.

| 순서 | 라벨 | 경로 | 아이콘 |
| --- | --- | --- | --- |
| 1 | 홈 | `/dashboard` | `LayoutGrid` |
| 2 | 통합 | `/dashboard/integrated` | `PanelsTopLeft` 또는 `LayoutDashboard` |
| 3 | 사업 | 현재 사업 칸반 또는 첫 사업 칸반 | `FolderKanban` |
| 4 | 더보기 | 시트 열기 | `MoreHorizontal` |

- 통합 탭은 `/dashboard/integrated`에서 활성 상태가 된다.
- 프로젝트가 없어도 홈·통합·더보기는 활성 링크로 남긴다.
- 더보기 시트에는 회의록, 현재 사업의 일정/아카이브, 사업 전환을 유지한다.
- 360px 폭에서 탭당 최소 72px을 확보하고, 레이블은 10px 고정 크기를 유지한다.

### 검증

- 360px, 390px, 768px에서 네 탭이 한 줄에 보인다.
- 통합 탭을 누르면 `/dashboard/integrated`로 이동한다.
- 회의록 화면에서는 더보기 탭이 활성으로 보인다.
- 현재 사업이 없어도 하단 바가 깨지지 않는다.

## 5. P1: 통합 API 오류 상태와 재시도

### 문제

API 요청 실패 시 로딩만 종료되어 빈 목록이 “결과 없음”처럼 렌더링된다.

### 구현

`src/app/dashboard/integrated/page.tsx`에 다음 상태를 추가한다.

```ts
const [error, setError] = useState<string | null>(null);
```

- 최초 조회와 필터 조회 시작 시 `error`를 초기화한다.
- `apiFetch` 실패는 `catch`에서 오류 상태로 변환한다.
- 오류 UI는 `AlertCircle` 아이콘, 짧은 메시지, `RotateCw` 아이콘 재시도 버튼으로 구성한다.
- 재시도는 현재 URL/필터 상태의 요청을 다시 실행한다.
- `더 불러오기` 실패는 기존 목록을 유지하고 버튼 영역에만 재시도 상태를 표시한다.

### 검증

- 네트워크 실패 시 빈 상태가 아닌 오류 상태가 보인다.
- 재시도 후 정상 결과로 전환된다.
- 다음 페이지 실패가 앞선 목록을 지우지 않는다.

## 6. P1: 필터 적용 모델 정합화

### 문제

사업 체크박스를 누르는 즉시 API 요청과 URL 변경이 발생하지만 버튼은 `적용`으로 표시된다.

### 구현

필터 시트에는 **임시 선택 상태**를 둔다.

```ts
const [draftProjectIds, setDraftProjectIds] = useState<string[]>([]);
```

동작:

1. 필터 시트를 열 때 `draftProjectIds`에 현재 적용 값을 복사한다.
2. 체크박스는 draft만 변경한다.
3. `적용`을 누르면 `selectedProjectIds`, URL, API 요청을 함께 갱신하고 시트를 닫는다.
4. 닫기·백드롭 클릭은 draft를 버리고 기존 필터를 유지한다.
5. `초기화`는 draft만 비운다. 이후 `적용`을 눌러야 반영된다.

### 검증

- 체크 후 닫으면 목록과 URL이 변경되지 않는다.
- 체크 후 적용하면 목록·URL·필터 점 표시가 함께 바뀐다.
- 초기화 후 닫으면 기존 필터가 유지된다.

## 7. P1: 유형 필터 계약 통일

### 문제

API는 `types=task,event`처럼 복수 유형을 허용하지만 UI는 단일 탭만 표현한다. 공유 URL의 복수 유형은 UI에서 `전체`로 해석된다.

### 결정

1차 구현은 **단일 유형**으로 제한한다. 다중 유형 선택 UX가 필요해질 때 별도 필터 시트로 확장한다.

### 구현

- API는 `type` 단일 쿼리 파라미터만 받는다: `all | task | event | archive | meeting`
- 허용하지 않는 값은 `all`로 정규화한다.
- URL은 `?type=task&range=7d` 형식으로 통일한다.
- UI 초기 상태와 탭 상태는 동일한 `type` 값만 읽고 쓴다.
- 기존 `types` 파라미터는 한 릴리스 동안 읽기 호환만 유지하거나 즉시 제거한다. 호환 유지 시 첫 유효 값 하나만 선택한다.

### 검증

- 각 탭 URL을 새 창에서 열면 같은 탭과 결과가 보인다.
- `types=task,event` 같은 이전 URL은 안전하게 `task` 또는 `all`로 정규화된다.
- API가 지원하지 않는 유형을 받아도 권한 범위 밖 데이터를 반환하지 않는다.

## 8. P2: 대량 데이터 조회와 커서 개선

### 문제

현재 API는 참여 사업의 모든 작업·일정·문서를 읽고 메모리에서 정렬한 뒤 offset으로 자른다. 데이터가 증가하면 첫 페이지도 느려지고 offset 커서가 신규 데이터에 의해 흔들릴 수 있다.

### 단계 1: 조회량 상한

초기 개선에서는 유형별로 `take: limit + 1`을 적용한다.

- 작업: `dueDate` 오름차순
- 일정: `startDate` 오름차순
- 문서/회의록: `updatedAt` 내림차순
- 각 유형의 최대 조회량은 `limit + 1` 또는 유형 수를 고려한 상한으로 제한한다.

조회 뒤 공통 배열을 정렬하고 화면 limit만 반환한다. 이 단계는 전체 스캔을 제한하지만 여러 유형의 완전한 전역 페이지네이션을 보장하지는 않는다.

### 단계 2: 안정 커서

전역 타임라인 요구가 확정되면 다음 중 하나를 선택한다.

1. **통합 Activity 모델 도입**
   - 작업·일정·문서 변경 시 `Activity` 레코드를 기록한다.
   - `occurredAt`, `type`, `sourceId`, `projectId` 복합 커서를 사용한다.
   - 가장 안정적인 전역 정렬·페이지네이션 방식이다.
2. **유형별 커서 병합**
   - 응답 커서에 작업/일정/문서별 마지막 키를 저장한다.
   - 각 유형을 독립적으로 조회하고 서버에서 병합한다.
   - 모델 추가 없이 가능하지만 구현 복잡도가 높다.

현재 시스템에는 변경 이력 요구가 없으므로 단계 1의 간단한 상한 적용부터 진행하고, Activity 모델은 감사 로그나 알림 요구와 함께 도입한다.

### 인덱스

Prisma 스키마에 다음 인덱스를 추가하는 마이그레이션을 준비한다.

```prisma
model Task {
  @@index([dueDate])
}

model Event {
  @@index([projectId, startDate])
}

model ArchivePost {
  @@index([projectId, kind, updatedAt])
  @@index([authorId, visibility])
}
```

### 검증

- 유형별 100개 이상 데이터에서 첫 페이지 응답이 전체 건수에 비례해 증가하지 않는다.
- 다음 페이지가 이전 페이지와 중복되지 않는다.
- 새 데이터가 추가되어도 현재 페이지의 링크가 다른 데이터로 바뀌지 않는다.

## 9. 구현 순서와 검증 명령

1. 기간 윈도우와 API 단위 테스트를 추가한다.
2. 모바일 통합 탭을 추가하고 360px/768px 화면을 확인한다.
3. 오류·재시도 상태를 추가한다.
4. 필터 draft/apply 모델과 단일 `type` URL 계약으로 전환한다.
5. 유형별 조회 상한, 인덱스 마이그레이션, 커서 회귀 검증을 진행한다.

각 단계마다 실행한다.

```bash
npx tsc --noEmit
npx eslint src/app/api/dashboard/integrated/route.ts src/app/dashboard/integrated/page.tsx src/components/BottomTabBar.tsx
git diff --check
```

## 10. 완료 기준

- `today`, `7d`, `30d`가 과거 데이터를 포함하지 않는다.
- 모바일·태블릿에서 통합 화면을 한 번의 하단 탭 동작으로 연다.
- 통합 API 실패가 빈 결과와 명확히 구분되고 재시도할 수 있다.
- 필터 시트의 변경은 적용을 누르기 전까지 목록과 URL을 바꾸지 않는다.
- URL, UI, API가 단일 유형 필터 계약을 공유한다.
- 첫 페이지 조회가 모든 데이터를 메모리로 가져오지 않는다.