# 온보딩 규정 반영 요약 및 개발 계획

작성일: 2026-09-13

## 1) 요청사항 요약

- 공개 링크 화면에서 작성자/날짜, 하단 아카이브 문구 제거
- 마크다운에서 2열 레이아웃/이미지 배치 자유도를 위해 HTML 태그 활용 가능하게 개선
- 팀 소개 문서 관리 권한을 현임 문화체육위원장 1인에게만 부여
- 슈퍼 어드민이 팀 소개 문서 관리자 계정을 지정/변경 가능해야 함
- 위원장 교체 시 기존 관리자 권한 회수 후 신규 위원장에게 단독 이전
- 일반 임원/구성원은 외부 링크 열람만 가능, 관리자 기능 접근 금지
- 슈퍼 어드민은 본인이 만들지 않은 사업/문서도 삭제 가능
- 통합 화면은 사업별 분산이 아닌 전체 사업 통합 관점으로 제공
- 통합 화면에서 사업명/카테고리/태그 필터 지원
- 통합 화면에서 칸반은 보드 뷰, 일정은 달력 뷰 제공
- 통합 화면에서 조회뿐 아니라 생성/편집까지 가능하도록 확장

## 2) 현재 구현 진단 (코드 기준)

### A. 공개 링크

- 공개 링크 페이지 존재 및 외부 접근 제어는 동작 중
- 2026-09-13 반영: 공개 링크에서 작성자/날짜, 하단 아카이브 문구 제거 완료

### B. 권한 모델

- 전역 권한은 User.role 문자열 기반(admin/member)으로만 운용
- 팀 소개 문서 전용 관리자 1인 지정 모델 없음
- 슈퍼 어드민과 일반 어드민의 분리 권한 없음
- 문서/프로젝트 삭제는 일부 API에서 작성자/소유자 중심 제약이 강함

### C. 마크다운 편집

- react-markdown + remark-gfm + remark-breaks + rehype-highlight 구성
- 현재 HTML 렌더링 확장(2열 div, img 속성 제어 등) 정책 미구현

### D. 통합 화면

- 전체 사업 통합 조회/필터(사업 단위)는 이미 구현
- 통합 화면의 칸반은 요약 스냅샷/리스트 성격이고, 직접 보드 편집은 미구현
- 통합 화면의 일정은 목록 중심이며 달력 뷰 직접 편집은 미구현
- 카테고리/태그 필터는 데이터 모델은 있으나 통합 화면 필터 UX/API 연동 미흡

## 3) 누락(락) 체크

### 필수 누락

- 팀 소개 문서 관리자 1인 지정/이전 도메인 모델
- 문화체육위원회 공간의 단일 space_admin 지정 UI/API
- 일반 구성원의 관리자 페이지 차단 라우팅/인가
- 위원장 교체 시 단일 관리자 보장 트랜잭션
- 슈퍼 어드민 삭제 권한의 전역 일관성 적용
- 통합 화면 내 칸반 보드 직접 생성/편집
- 통합 화면 내 일정 달력 직접 생성/편집
- 통합 화면 카테고리/태그 필터 완결

### 정책상 주의 항목

- 동시에 2명 이상 관리자 부여 방지 (DB 제약 + 트랜잭션 필수)
- HTML 허용 시 XSS 방어 (허용 태그/속성 화이트리스트)
- 외부 링크 열람자와 내부 사용자의 권한 경계 명확화

## 4) 개발 계획 (권장 단계)

## Phase 1. 권한/데이터 모델 정비

목표:
- 팀 소개 문서 관리자 1인 정책을 서버/DB 레벨에서 강제

작업:
- Prisma에 전역 역할 체계 확장
  - admin(개발자/운영자), space_admin(문화체육위원회 관리), member
- 문화체육위원회 단일 space_admin 설정 저장 모델 추가
  - 예: SpaceAdministration(spaceAdminUserId)
- 단일 관리자 보장 규칙 추가
  - 이전 시 기존 권한 회수 + 신규 부여를 단일 트랜잭션 처리

완료 기준:
- 팀 소개 관리자 계정은 항상 0 또는 1명
- 관리자 변경 API는 admin만 호출 가능

## Phase 2. 인가 미들웨어/API 가드 통일

목표:
- 페이지 접근, API 접근, 삭제 권한을 규정대로 통일

작업:
- requireAdmin, requireSpaceAdmin, requireProjectDeletePermission 유틸 추가
- 팀 소개 관리 API/페이지는 팀 소개 관리자만 접근 허용
- super_admin은 사업/문서 삭제 전역 허용
- 일반 사용자에게는 관리자 엔드포인트 일괄 403

완료 기준:
- 권한 매트릭스 테스트 통과
- 권한 우회 요청 시 401/403 일관 반환

## Phase 3. 공간 관리자 인수인계 화면 구현

목표:
- 현임 space_admin이 후임 문화체육위원장을 지정/변경

작업:
- 관리자 계정 검색/선택 UI
- 현재 관리자 표시, 변경 이력(최소 최근 1건) 노출
- 변경 확인 모달 및 원자적 저장

완료 기준:
- 현임 space_admin에서 후임 space_admin 지정/변경 가능
- 변경 즉시 기존 관리자 접근 차단, 신규 관리자 접근 허용

## Phase 4. 마크다운 HTML 확장

상세 구현 계획은 `MARKDOWN_ENHANCEMENT_PLAN.md`를 따른다.

목표:
- 2열 레이아웃/이미지 배치 유연성 제공

작업:
- rehype-raw + rehype-sanitize 도입
- 허용 태그/속성 화이트리스트 정의
  - div, section, img, figure, figcaption, span, br 등
  - class, style(제한), width, height, alt 등 선별 허용
- 에디터 도움말에 2열 템플릿/이미지 예시 제공

완료 기준:
- HTML 사용 시 의도한 레이아웃 렌더링
- 스크립트/위험 속성 차단

## Phase 5. 통합 화면 편집형 고도화

목표:
- 통합 화면에서 조회뿐 아니라 생성/편집까지 수행

작업:
- 칸반: 통합 보드 뷰 제공 (프로젝트 라벨 포함), 드래그 이동/생성/수정
- 일정: 통합 달력 뷰 제공 (프로젝트 색상), 일정 생성/수정/삭제
- 필터: 사업명/카테고리/태그 다중 필터를 URL 상태와 동기화

완료 기준:
- 사용자가 통합 화면에서 칸반/일정 생성 및 편집 가능
- 필터 조합 공유 URL 재진입 시 동일 상태 복원

## 5) 테스트/검증 계획

- 권한 테스트
  - admin / space_admin / 일반 구성원 / 외부 링크 사용자
- 보안 테스트
  - HTML 삽입 XSS 시도 차단
- 회귀 테스트
  - 기존 프로젝트/문서 CRUD, 공개 링크 열람, 통합 화면 조회
- 인수인계 시나리오 테스트
  - 23기 -> 24기 관리자 이전 과정에서 동시 관리자 2인 미발생

## 6) 실행 우선순위 제안

1. Phase 1~2 (권한 모델 + API 가드)
2. Phase 3 (슈퍼 어드민 지정 화면)
3. Phase 4 (마크다운 HTML 확장)
4. Phase 5 (통합 화면 편집형)

권한 경계가 먼저 고정되어야 이후 UI 기능(편집/삭제/관리)을 안전하게 붙일 수 있음.

## 7) 2026-09-13 확정 권한 정책

- `admin`: 개발자/운영자 전역 권한. 여러 명 허용.
- `space_admin`: 문화체육위원회 공간 전용 권한. 정확히 한 명만 허용.
- 초기 `space_admin`: finerel5955@gmail.com.
- 관리자 교체는 예약 없이 즉시 처리하고, 기존 관리자 회수와 신규 관리자 부여를 하나의 트랜잭션으로 처리.
- 이전 로그는 실행자, 실행 시각, 이전 관리자, 신규 관리자를 보관.
- 관리자 이전은 현임 `space_admin`만 실행할 수 있다. `admin`은 개발자/운영자 전역 권한이지만, 일반 운영 화면에서는 이 인수인계 기능을 노출하지 않는다.
- `space_admin`은 본인이 작성하거나 소유하지 않은 문화체육위원회 사업·아카이브도 수정·삭제하고, 사업 참여자와 초대를 관리할 수 있다.
- 외부 공개 링크 사용자는 공개 문서 읽기만 가능하며, 내부 문서 목록·관리 화면·수정 API에는 접근할 수 없다.
