# 통합 화면 사용성 개선 리포트

작성일: 2026-08-23

## 1. 검토 목적

통합 화면이 여러 사업의 상태를 빠르게 파악하고 필요한 상세 화면으로 이동하는 허브 역할을 하는지, 특히 모바일 사용 흐름을 중심으로 점검했다.

검토 범위는 `/dashboard/integrated`, 통합 집계 API, 모바일 하단 탭이다.

## 2. 반영된 개선 사항

| 항목 | 적용 내용 | 사용자 효과 |
| --- | --- | --- |
| 최근 업데이트 분리 | 최근 변경 항목을 개요 최상단의 독립 섹션으로 구성 | 전체 현황과 최근 변경을 혼동하지 않음 |
| 최근 업데이트 제한 | 최대 3개만 표시 | 모바일 첫 화면의 길이와 정보 밀도 제어 |
| 섹션별 데이터 조회 | 칸반, 일정, 문서 목록을 공용 타임라인이 아닌 독립 응답으로 조회 | 한 유형의 데이터가 많아도 다른 섹션이 비어 보이지 않음 |
| 일정 정렬 | 다가오는 일정은 시작일 오름차순으로 조회 | 가까운 일정을 먼저 판단 가능 |
| 전체 보기 이동 | 섹션의 전체 보기가 해당 유형의 전체 기간 목록으로 이동 | 요약과 상세 목록의 역할이 분명해짐 |
| 모바일 통합 진입 | 사업 상세 화면 하단 탭에도 통합 화면 링크 유지 | 사업 내부 작업 중에도 전체 현황으로 즉시 복귀 가능 |
| 개요 일정 필터 제거 | 개요 헤더에서 날짜 범위 선택을 제거 | 개요의 제어 수를 줄이고 빠른 훑기 목적을 유지 |
| 개요 일정 기준 | 개요 일정은 항상 다음 7일로 조회하고 범위를 함께 표시 | 필터 없이도 일정의 시간 범위를 명확히 이해 가능 |
| 수동 새로고침 | 개요 헤더에 현재 사업 필터를 유지하는 새로고침 제어 추가 | 화면 재진입 없이 최신 현황을 다시 확인 가능 |
| 최근 업데이트 시각 | 당일 변경에는 시간, 이전 변경에는 날짜를 표시 | 같은 날의 최신 순서를 빠르게 판단 가능 |
| 최근 업데이트 균형 | 후보에 다른 유형이 있으면 한 유형은 최대 2개까지만 우선 표시 | 3개 요약에 작업, 일정, 문서의 흐름을 함께 보여 줄 가능성 증가 |
| 모바일 유형 탭 | 양끝 여백과 스냅 스크롤을 적용 | 작은 화면에서 처음과 마지막 탭에 안정적으로 접근 가능 |
| 빈 사업 상태 | 참여 가능한 사업이 없는 경우에도 완전한 빈 요약 응답 반환 | 빈 상태에서 화면 오류 없이 안내 가능 |

## 3. 현재 사용 흐름

### 모바일

1. 하단의 `통합` 탭으로 전체 사업 현황에 진입한다.
2. 상단의 유형 탭으로 칸반, 일정, 문서, 회의록 상세 목록으로 전환한다.
3. 개요에서는 최근 업데이트 3개, 칸반 현황, 다가오는 일정, 최근 문서·회의록을 순서대로 확인한다.
4. 각 섹션의 `전체 보기`에서 선택 유형의 전체 기간 목록으로 이동한다.
5. 사업 필터는 하단 시트에서 임시 선택 후 `적용`해야 반영된다.

### 데스크톱

1. 사이드바의 `통합 화면`으로 진입한다.
2. 동일한 개요와 유형 탭을 사용한다.
3. 넓은 화면에서는 일정과 문서·회의록을 두 열로 비교한다.

## 4. 사용성 평가

### 잘 작동하는 점

- 최근 변경과 전체 현황을 분리해 첫 화면의 우선순위가 명확하다.
- 사업 색상, 사업명, 날짜를 항목에 함께 표시해 사업 간 맥락을 잃지 않는다.
- 칸반은 사업별 실제 열 이름을 유지하므로 서로 다른 업무 흐름을 임의로 합치지 않는다.
- 모든 요약 섹션에 상세 경로가 있어, 요약 화면이 막다른 화면이 되지 않는다.
- 긴 목록을 첫 화면에 모두 노출하지 않아 모바일 스크롤 부담을 줄인다.

### 후속 관찰 항목

- 최근 업데이트의 유형 균형이 실제 업무 흐름을 왜곡하지 않는지 운영 데이터로 확인한다.
- 360px, 390px에서 마지막 유형 탭까지의 스크롤 접근성을 시각 검증한다.

## 5. 검증 결과와 다음 기준

정적 검증은 통과했다.

- TypeScript: `npx tsc --noEmit`
- 대상 ESLint: 통합 API 및 통합 화면 컴포넌트
- 공백 검사: `git diff --check`

다음 UI 변경 전후에는 아래 뷰포트에서 직접 확인한다.

- 모바일: 360x800, 390x844
- 태블릿: 768x1024
- 데스크톱: 1440x900

확인 항목은 다음과 같다.

- 하단 탭과 마지막 섹션이 겹치지 않는다.
- 유형 탭과 사업 필터가 터치하기 충분한 크기로 표시된다.
- 긴 사업명과 제목이 날짜, 사업명, 상세 이동 아이콘을 밀어내지 않는다.
- 최근 업데이트는 최대 3개이며, 당일 항목은 시간까지 표시되고 일정은 가까운 순서로 표시된다.
- 빈 사업 상태, 네트워크 오류, 필터 적용 취소 상태가 정상 빈 결과와 구분된다.