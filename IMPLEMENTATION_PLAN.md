# 문화체육위원회 업무 시스템 기능 구현 계획안

## 1. 기본 권한 정책

아카이브 공개 범위는 게시물 단위로 관리한다.

| 공개 범위 | 열람 가능 사용자 | 편집 가능 사용자 | 링크 접근 |
| --- | --- | --- | --- |
| 비공개 | 작성자 본인 | 작성자 본인 | 불가 |
| 내부 공유 | 해당 사업의 수락된 참여자 | 해당 사업의 수락된 참여자 | 로그인 필요 |
| 외부 공유 | 공유 링크를 가진 외부 사용자 | 없음 | 링크를 아는 사용자 |

- 외부 공유는 읽기 전용이며, 공개 링크를 폐기하면 즉시 접근할 수 없도록 한다.
- 초대 수락 전 사용자는 해당 사업의 내부 아카이브와 업무 데이터에 접근하지 못한다.
- 사업에 참여하지 않는 가입 사용자는 사업 목록과 공개된 요약 정보만 확인할 수 있고, 칸반·일정·아카이브 등의 세부 내용은 확인할 수 없다.
- 비참여자에게 제공하는 요약 정보는 사업 진행 상태, 참여 인원, 한 줄 설명으로 제한한다.
- 사업 소유자는 참여자 관리와 사업 공유 방식 설정을 담당한다.
- 기수와 사업의 카테고리·태그는 별도의 행정 권한을 가진 사용자만 관리한다.
- 기존 `published` 값은 마이그레이션 기간 동안 외부 공유 여부와 호환하되, 최종적으로는 명시적인 공개 범위 값으로 통합한다.

## 2. 이번 주 구현 범위: 사업 관리 및 회원·사업 공유

### 2-1. 데이터 모델 및 마이그레이션

1. `User`에 `cohortId`를 추가하고 `Cohort`와 관계를 맺는다.
   - 가입 시 관리자가 활성화한 기수 중 하나를 필수 선택
   - 기존 회원은 `미지정` 상태로 두고 행정 권한 사용자가 보완
2. 기수 관리를 위해 `Cohort` 모델을 추가한다.
   - `name`, `isActive`, `order`, `createdAt`, `updatedAt`
   - 기수 생성·수정·비활성화·표시 순서 변경은 행정 권한 사용자만 가능
   - 비활성화된 기수는 신규 가입 선택지에서 제외하되 기존 회원의 기록은 유지
3. 전역 행정 권한을 명확히 분리한다.
   - 기존 `User.role`의 `admin` 값을 전역 행정 권한으로 사용한다.
   - `admin`은 모든 사업의 기수, 카테고리, 태그 및 사업 메타데이터 관리 권한을 가진다.
   - 사업별 별도 행정 권한은 두지 않는다.
   - 사업 소유자는 참여자·공유 방식은 관리하지만 카테고리·태그 정책을 임의로 변경하지 못한다.
4. `Project`에 다음 필드를 추가한다.
   - `order`: 사업 목록 표시 순서
   - `sharingMode`: `PUBLIC`(기본값) 또는 `INVITE_ONLY`
   - `categoryId`와 태그 관계를 후속 통합 화면 작업 전에 추가
5. 초대 흐름을 위해 `ProjectInvitation` 모델을 추가한다.
   - `projectId`, `inviteeId`, `invitedBy`, `status`, `expiresAt`, `respondedAt`
   - 상태: `PENDING`, `ACCEPTED`, `DECLINED`, `CANCELED`
   - `expiresAt`은 초대 생성 시점부터 정확히 7일 후로 설정
   - 동일 사업·사용자에 대한 중복 대기 초대 방지
6. 마이그레이션 후 기존 사업은 `PUBLIC`, 기존 멤버는 `ACCEPTED`로 변환한다.

구현 방식은 현재 프로젝트의 Prisma 스키마와 일관되게 문자열 상태값을 사용한다. 다만 기수·공개 방식·초대 상태·아카이브 공개 범위처럼 오탈자가 데이터 권한에 영향을 주는 값은 서버의 허용 목록 상수로 검증하고, DB에는 인덱스를 추가한다.

### 2-2. 회원가입

- 회원가입 폼에 기수 선택 필드를 추가한다.
- 서버에서 행정 권한 사용자가 활성화한 기수인지 검증하고, 클라이언트 값만 신뢰하지 않는다.
- 가입 완료 후 세션과 사용자 정보에 기수가 반영되는지 확인한다.
- 기수는 별도 `Cohort` 테이블에 저장하고, 기수 관리 화면에서 생성·수정·비활성화·순서 변경을 제공한다.
- 기수 관리 API는 행정 권한이 없는 요청을 `403`으로 거부한다.

### 2-3. 사업 공유 및 초대

- 사업 생성 시 공유 방식은 `전체 공개`를 기본값으로 한다.
- 전체 공개 사업은 가입 사용자 모두에게 사업 목록과 요약 정보만 노출한다.
   - 요약 정보: 사업명, 카테고리, 태그, 사업 진행 상태, 참여 인원, 한 줄 설명
   - 세부 정보: 칸반, 일정, 내부 아카이브, 참여자 상세는 수락된 참여자만 접근
- 비참여자용 API와 참여자용 상세 API를 분리해 응답 데이터 자체에서 내부 정보가 노출되지 않도록 한다.
- 초대 방식 사업은 소유자가 특정 가입 사용자를 초대한다.
- 초대받은 사용자는 생성 후 7일 안에 `수락` 또는 `거절`할 수 있고, 수락 시에만 `ProjectMember`를 생성한다.
- 7일이 지나면 `PENDING` 초대는 만료 처리되며 수락할 수 없다. 소유자는 만료된 사용자에게 새 초대를 보낼 수 있다.
- 초대 대기·거절·취소 상태를 화면에 표시한다.
- 기존 담당자 지정 기능은 `ProjectMember.role`을 유지해 사업별 참여자와 담당 역할을 관리한다.
- 내부 공유 사업의 아카이브는 수락된 내부 참여자 모두에게 열람·편집 권한을 제공한다.
- 비공개 아카이브는 작성자만 열람·편집할 수 있고, 외부 공유 아카이브는 외부 사용자에게 읽기만 허용한다.
- 사업명·카테고리·태그 수정 권한은 다음처럼 분리한다.
   - 사업명: 사업 소유자 또는 행정 권한 사용자
   - 카테고리·태그: 전역 행정 권한 사용자
   - 참여자·공유 방식: 사업 소유자
- 추천 API 구성:
  - `POST /api/projects/:projectId/invitations`
  - `GET /api/invitations`
  - `PATCH /api/invitations/:invitationId` (`accept` / `decline`)
  - `PATCH /api/projects/:projectId` (`sharingMode` 포함)
   - `/api/admin/cohorts` (행정 권한 전용)
   - `/api/admin/project-metadata` (카테고리·태그 관리 전용)

### 2-4. 사업 목록 순서 변경 및 삭제

- 대시보드 사이드바 또는 사업 관리 화면에서 드래그 앤 드롭으로 순서를 변경한다.
- 순서 저장은 전체 목록의 ID와 순서를 한 번에 전송하고 서버에서 트랜잭션으로 갱신한다.
- 목록 조회는 `order` 오름차순, 동률일 때 `createdAt`을 보조 정렬 기준으로 사용한다.
- 삭제는 소유자만 실행할 수 있도록 한다.
- 삭제 전 확인 모달에 칸반·일정·아카이브·참여자 데이터가 함께 삭제된다는 점을 명시한다.
- 기존 삭제 API의 소유자 검증과 cascade 삭제를 재사용하고, 프론트 목록에서 성공 후 즉시 제거한다.

### 2-5. 이번 주 완료 기준

- 신규 가입 시 기수를 선택하지 않으면 제출할 수 없다.
- 신규 가입 시 행정 권한 사용자가 활성화한 기수만 선택할 수 있다.
- 새 사업은 전체 공개로 생성된다.
- 소유자가 전체 공개/초대 방식을 변경할 수 있다.
- 초대 사용자가 7일 안에 수락·거절할 수 있고, 기간이 지나면 수락할 수 없다.
- 비참여자는 사업 목록과 요약 정보만 확인할 수 있고 세부 내용은 확인할 수 없다.
- 비참여자에게 노출되는 요약 정보가 진행 상태·참여 인원·한 줄 설명으로 제한된다.
- 전역 행정 권한 사용자가 기수·카테고리·태그를 관리할 수 있고 일반 사용자나 사업 참여자는 이를 변경할 수 없다.
- 내부 공유 사업의 수락된 참여자 모두가 아카이브를 편집할 수 있다.
- 사업 목록 순서가 새로고침 후에도 유지된다.
- 소유자가 사업을 삭제할 수 있으며 다른 참여자는 삭제할 수 없다.
- 권한 우회 요청에 대해 API가 `401` 또는 `403`을 반환한다.

## 3. 후속 구현 1: 통합 화면

1. 대시보드에 전체 사업을 집계하는 통합 화면을 추가한다.
2. 사업별로 칸반, 일정, 아카이브를 구분할 수 있도록 섹션 또는 탭을 둔다.
3. 사업명, 카테고리, 태그, 진행 상태를 기준으로 서버 또는 클라이언트 필터링을 지원한다.
4. 카테고리·태그 데이터 모델을 먼저 확정한다.
   - 카테고리: 사업당 하나의 값
   - 태그: 사업당 여러 개의 값
5. 비참여자에게는 요약 API만 제공하고, 참여자용 상세 API와 분리한다.
6. 데이터가 많아질 경우 모든 상세 데이터를 한 번에 내려받지 않는다.

## 4. 후속 구현 2: 아카이브 개선 및 외부 공유

### 데이터 및 권한

- `ArchivePost.visibility`를 `PRIVATE`, `INTERNAL`, `EXTERNAL` 중 하나로 관리한다.
- 외부 공유용으로 별도의 예측 불가능한 `shareToken`과 `shareEnabled`를 둔다.
- 외부 공유 링크에는 비밀번호와 만료일을 두지 않는다.
- 공개 API는 `shareEnabled = true`이고 `visibility = EXTERNAL`인 게시물만 반환한다.
- 내부 API는 작성자 또는 수락된 사업 참여자만 반환한다.
- 공개 링크는 원래 편집 API를 사용할 수 없고, 읽기 전용 렌더링만 제공한다.
- 링크 재생성 또는 폐기 기능만 제공해 사용자가 원할 때 유출된 링크를 무효화할 수 있게 한다.

### 화면

- 현재 마크다운 편집 기능을 유지하면서 제목, 본문, 저장 상태, 공개 범위를 한 화면에서 자연스럽게 조작한다.
- 게시물 목록에는 공개 범위와 작성자를 표시한다.
- 비공개 초안, 내부 공유 문서, 외부 공유 문서를 명확히 구분한다.
- 공유 범위 변경 시 현재 권한과 링크 상태를 함께 안내한다.

## 5. 후속 구현 3: 회의록

- 아카이브 게시물에 `kind`를 추가해 일반 문서와 회의록을 구분한다.
- 회의록 전용 섹션에서는 사업명, 회의일, 작성자, 제목, 공개 범위를 기준으로 전체 목록을 조회한다.
- 회의록 상세 화면은 기존 아카이브 편집기를 재사용하고, 사업 링크를 제공한다.
- 통합 화면에서는 회의록을 아카이브의 하위 유형으로 표시해 별도 데이터가 중복되지 않도록 한다.
- 회의록에 필요한 최소 필드가 확정되면 `meetingDate`, 참석자, 안건을 단계적으로 추가한다.

## 6. 권장 작업 순서

1. 공개 범위·사업 공유 방식·초대 상태 용어 확정
2. Prisma 스키마와 마이그레이션 작성
3. 기수·행정 권한 모델과 관리자 관리 화면 구현
4. 회원가입 기수 선택 및 서버 검증
5. 사업 공유 방식과 7일 만료 초대 API 구현
6. 초대 수락·거절 화면 구현
7. 사업 목록 순서 저장 및 삭제 UI 연결
8. 권한 회귀 테스트와 기존 데이터 마이그레이션 검증
9. 통합 화면용 카테고리·태그 설계
10. 아카이브 공개 범위와 외부 공유 링크 구현
11. 회의록 분류 및 전용 섹션 구현

## 7. 먼저 결정하면 좋은 사항

- 관리자 계정의 생성·해제는 기존 슈퍼 관리자 또는 운영 DB에서 어떻게 통제할 것인가
- 만료된 초대를 언제 정리할 것인가: 조회 시 지연 처리하거나 주기적 작업으로 정리

## 8. 예상 검증 항목

- 권한 매트릭스: 작성자, 사업 소유자, 수락된 참여자, 초대 대기자, 일반 가입자, 비로그인 사용자
- 행정 권한 사용자와 일반 사용자 간 기수·카테고리·태그 관리 권한 분리
- 기수가 DB에서 생성·수정·비활성화된 뒤 회원가입 선택지에 즉시 반영되는지 확인
- 기존 사업·기존 회원 데이터 마이그레이션
- 초대 중복, 7일 만료, 거절 후 재초대, 마지막 소유자 삭제 방지
- 비참여자 응답에 칸반·일정·아카이브 상세 데이터가 포함되지 않는지 확인
- 비참여자 응답에 사업 진행 상태·참여 인원·한 줄 설명만 포함되는지 확인
- 내부 공유의 수락된 참여자 모두가 아카이브를 편집할 수 있는지 확인
- 사업 순서 동시 저장과 새로고침 유지
- 사업 삭제 시 관련 칸반·일정·아카이브 cascade 처리
- 외부 링크의 읽기 전용 보장과 링크 폐기 즉시 반영
- 외부 링크에 비밀번호·만료일을 요구하지 않으며 폐기와 재생성이 정상 동작하는지 확인

## 9. 구현 적합도 및 예상 문제 재검토

### 구현 적합도

- **데이터베이스**: 현재 Prisma 7과 SQLite/Turso 구성이 `Cohort`, 초대, 프로젝트 메타데이터, 아카이브 공개 범위를 추가하기에 적합하다. 기존 관계형 모델과 cascade 삭제 패턴도 재사용할 수 있다.
- **인증·권한**: NextAuth JWT 세션은 사용자 ID만 세션에 보관하고, 행정 권한과 사업 참여 여부는 요청 시 DB에서 확인하는 방식이 적합하다. 권한 변경 후 재로그인 없이도 즉시 반영된다.
- **드래그 앤 드롭**: 이미 `@dnd-kit`이 설치되어 있어 사업 순서 변경 UI는 새 라이브러리 없이 구현할 수 있다.
- **아카이브 편집**: 기존 마크다운 편집기를 유지하면서 공개 범위와 편집 권한을 API에서 통제하는 방식이 가장 적합하다.

### 구현 시 예상 문제와 대응

1. **기존 사업 목록 API의 접근 범위 충돌**
   - 현재 `/api/projects`는 `ProjectMember`인 사업만 조회한다.
   - 대응: `PUBLIC` 사업의 요약 목록 API와 참여자용 상세 API를 분리한다. 기존 상세 API에 조건만 추가해 내부 데이터가 섞여 내려가지 않도록 한다.

2. **사업 상태와 한 줄 설명의 기존 필드 부재**
   - 현재 `Project`에는 설명 필드만 있고 진행 상태나 한 줄 설명 필드가 없다.
   - 대응: `summary`와 `status`를 `Project`에 추가한다. 기존 `description`은 상세 설명으로 유지하고, 비참여자 요약 응답에는 `summary`, `status`, 참여 인원 수만 반환한다.

3. **참여 인원 수 노출과 개인정보 문제**
   - 숫자만 공개하고 참여자 이름·이메일·역할 목록은 비참여자 응답에서 제외한다.
   - 대응: 목록 API에서 `_count`만 사용하고 `members` 관계 데이터는 포함하지 않는다.

4. **전역 어드민 권한의 최초 지정**
   - 최초 어드민은 운영 DB에서 `User.role = 'admin'`으로 직접 지정한다.
   - 대응: 애플리케이션에 자기 권한 승격 기능을 만들지 않는다. 이후 어드민 지정·해제도 DB 운영 절차로 관리하며, 모든 관리자 API는 매 요청마다 DB의 `User.role`을 확인한다.

5. **JWT 세션에 권한을 캐시할 때의 stale 권한 위험**
   - admin 여부를 JWT에만 저장하면 DB에서 권한을 해제해도 기존 세션이 계속 유효할 수 있다.
   - 대응: 세션에는 ID만 사용하고 `assertAdmin` 헬퍼가 DB에서 최신 role을 조회한다.

6. **초대 만료의 시간대와 경계값**
   - 서버와 클라이언트의 시간대가 다르면 7일 경계에서 수락 가능 여부가 다르게 보일 수 있다.
   - 대응: DB에는 UTC `DateTime`을 저장하고, 수락 API에서 서버 시간으로 `expiresAt > now`를 최종 검증한다. 화면의 남은 기간 표시는 보조 정보로만 사용한다.

7. **초대 수락과 멤버 생성의 중복 처리**
   - 같은 초대를 동시에 수락하면 중복 멤버 생성 또는 상태 불일치가 발생할 수 있다.
   - 대응: 초대 상태 변경과 `ProjectMember` 생성을 하나의 DB 트랜잭션으로 처리하고, 기존 복합 unique 제약을 유지한다.

8. **내부 아카이브 편집 권한의 일관성**
   - 현재 아카이브 API는 사업 멤버 여부를 기준으로 접근하므로, 공개 범위별 편집 규칙을 별도로 적용해야 한다.
   - 대응: `canReadArchive`와 `canEditArchive`를 분리한다. `INTERNAL`은 수락된 참여자 모두 편집, `PRIVATE`는 작성자만 편집, `EXTERNAL`은 인증 여부와 관계없이 읽기 전용으로 처리한다.

9. **기존 `published` 데이터와 새 공개 범위의 변환**
   - 기존 게시물의 `published` 의미가 외부 공개와 내부 노출을 함께 포함할 가능성이 있다.
   - 대응: 마이그레이션에서 `published = true`는 `EXTERNAL`, `false`는 작성자 중심의 `PRIVATE`로 우선 변환하고, 내부 공유가 필요한 게시물은 운영자가 확인할 수 있는 보정 목록을 제공한다.

10. **SQLite/Turso에서 목록 순서 동시 저장**
    - 여러 관리 화면에서 동시에 순서를 저장하면 마지막 요청이 앞선 변경을 덮을 수 있다.
    - 대응: 서버 트랜잭션으로 순서를 갱신하고, 초기 구현은 마지막 저장 우선으로 처리한다. 필요해질 경우 `updatedAt` 또는 버전 검증을 추가한다.

### 재검토 후 확정 작업 순서

1. 운영 DB에서 최초 어드민 지정
2. `Cohort` 관계, `Project.summary`·`Project.status`, 공개 방식, 초대 모델 스키마 및 마이그레이션
3. `assertAdmin`, `canReadProject`, `canReadArchive`, `canEditArchive` 권한 헬퍼
4. 관리자용 기수·카테고리·태그 API와 회원가입 기수 조회 API
5. 비참여자 요약 목록과 참여자 상세 목록 API 분리
6. 사업 공유 방식, 7일 초대, 수락·거절 및 트랜잭션 처리
7. 사업 순서 변경과 삭제 UI 연결
8. 공개 범위별 아카이브 API와 외부 읽기 전용 링크
9. 권한·마이그레이션·동시성 회귀 테스트
