# 보안 강화 실행 계획

작성일: 2026-09-13  
상태: 계획 수립 완료 — 구현 전

## 1. 목적과 범위

이번 작업은 재검토에서 확인한 아래 네 가지 위험을 우선순위대로 줄인다.

1. 로그인·회원가입·계정 조회에 대한 무차별 대입 및 자동화 요청 방지
2. 공개 이메일 찾기 기능을 통한 계정 존재 및 이메일 일부 노출 제거
3. 전 응답의 기본 보안 헤더 적용
4. 회원가입과 프로젝트 생성·수정 요청의 서버 측 크기·형식 검증 통일

### 완료 기준

- 같은 IP 또는 동일 이메일을 반복 사용한 로그인·가입·계정 조회 요청이 제한되고 `429 Too Many Requests`와 재시도 시간을 받는다.
- 비로그인 사용자가 이름만으로 계정 존재, 이메일 일부, 계정 수를 알 수 없다.
- CSP, 클릭재킹 방지, MIME 스니핑 방지, Referrer 정책이 모든 앱 응답에 적용된다.
- 클라이언트를 우회해 API를 직접 호출해도 잘못된 형식·과도한 길이·허용되지 않은 값은 `400`으로 거부된다.
- 기존 로그인, 가입, 프로젝트 생성·수정, Markdown 미리보기·공개 링크가 회귀 없이 동작한다.

### 이번 범위에서 제외

- 비밀번호 재설정 및 이메일 인증 전체 구현
- CAPTCHA 도입
- 사용자별 세션 폐기 목록과 강제 로그아웃
- 이미지 재인코딩·EXIF 제거
- Prisma CLI가 끌어오는 `deepmerge-ts`, `mysql2` 감사 항목의 강제 해결

마지막 항목은 현재 Prisma 7 계열에서 npm이 Prisma 6으로 다운그레이드하는 방법만 제시하므로, 별도 호환성 검증 없이 적용하지 않는다.

## 2. 확정할 보안 정책

### 2-1. 요청 제한 저장소

서버 메모리 기반 카운터는 개발 서버에서는 동작해도 Vercel의 다중 함수 인스턴스와 재시작 환경에서 우회된다. 프로덕션 공용 저장소를 사용한다.

권장안:

- Upstash Redis와 `@upstash/ratelimit`을 사용한다.
- 환경 변수는 `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`으로 관리한다.
- 로컬 개발에서는 메모리 저장소를 명시적으로 선택하고, 프로덕션에서는 설정 누락 시 인증·가입·이메일 찾기 엔드포인트를 fail closed(`503`) 처리한다.
- 원본 IP는 배포 플랫폼이 설정한 `x-forwarded-for`의 첫 번째 값만 사용한다. 임의 클라이언트가 직접 해당 헤더를 신뢰하게 되는 비프록시 배포는 지원하지 않는다.
- IP를 원문 그대로 장기간 저장하지 않는다. rate-limit key에는 서버 비밀값을 이용한 HMAC 해시를 사용하고, Redis TTL 만료 뒤 식별자를 남기지 않는다.

배포 전에는 Vercel 프로젝트의 환경 변수에 Redis 자격 증명을 추가하고 Preview/Production 환경 모두에서 검증한다.

### 2-2. 엔드포인트별 제한값

제한값은 초기 보호값이다. 출시 후 `429` 비율과 정상 사용자 영향도를 보고 조정한다.

| 흐름 | 적용 위치 | 키 | 초기 한도 | 초과 응답 |
| --- | --- | --- | --- | --- |
| 로그인 | Credentials `authorize` 직전 | IP + 정규화 이메일 | IP 10회/10분, 이메일 5회/10분 | `429`, `Retry-After` |
| 로그인 성공 | Credentials `authorize` 직후 | IP + 정규화 이메일 | 해당 키의 실패 카운터 초기화 | 정상 로그인 |
| 회원가입 | `POST /api/register` | IP + 정규화 이메일 | IP 5회/1시간, 이메일 3회/24시간 | `429`, `Retry-After` |
| 이메일 찾기 | `POST /api/find-email` | IP | 3회/1시간 | `429`, `Retry-After` |

- IP 제한과 이메일 제한을 모두 검사해 둘 중 하나라도 초과되면 거부한다.
- 입력값은 검사 전에 trim, Unicode 정규화, 소문자화해 키 우회를 막는다.
- 실패 사유와 남은 시도 횟수는 응답 본문에 노출하지 않는다.
- `429`는 표준 `Retry-After` 헤더 및 일반화된 한국어 오류 메시지를 함께 반환한다.
- rate-limit 저장소 장애는 로그인·가입·이메일 찾기에 `503`을 반환하고, 일반 프로젝트 API에는 영향을 주지 않는다.

## 3. 구현 순서

## Phase S1 — 공통 보안 기반

### 목표

각 API에 제각각 구현하지 않도록 요청 식별, 제한, 응답, 검증의 공통 모듈을 먼저 만든다.

### 변경 대상

- `src/lib/rate-limit.ts` 신규
- `src/lib/validation.ts` 신규
- `.env.example`
- `package.json`, `package-lock.json`

### 세부 작업

1. Redis 기반 구현체와 로컬 개발 전용 메모리 구현체를 감싼 `checkRateLimit()` 인터페이스를 만든다.
2. `getClientIp()`는 신뢰할 수 있는 프록시 환경의 `x-forwarded-for` 첫 값을 파싱하고, 값이 없거나 비정상이면 고정된 unknown 버킷을 사용한다.
3. `normalizeEmail()`은 문자열 타입, trim, NFC 정규화, 소문자화, 최대 길이, 기본 이메일 형식을 검사한다.
4. HMAC 기반 `rateLimitKey()`로 IP·이메일 원문이 Redis 키나 로그에 남지 않게 한다.
5. `rateLimitedResponse()`는 `429`, `Retry-After`, `Cache-Control: no-store`를 일관되게 반환한다.
6. 검증 모듈에 문자열 길이·열거형·색상·태그·배열 수를 위한 재사용 가능한 안전 함수와 상수를 둔다.
7. `.env.example`에 Upstash 환경 변수와 운영 환경의 필수 설정 설명을 추가한다.

### 테스트

- 같은 키가 한도 미만이면 허용되고, 한도 초과 시 `429`와 올바른 재시도 시간이 반환된다.
- 대소문자·공백·Unicode 변형 이메일은 하나의 키로 합쳐진다.
- IP 또는 이메일 중 하나만 초과해도 거부된다.
- Redis 설정이 없는 production 모드에서는 민감 엔드포인트가 `503`이 된다.
- 로컬 개발용 저장소는 테스트마다 초기화된다.

## Phase S2 — 로그인·회원가입 보호

### 목표

비밀번호 대입과 자동 계정 생성을 차단하고 서버에서 가입 입력을 엄격히 검증한다.

### 변경 대상

- `src/auth.ts`
- `src/app/api/register/route.ts`
- `src/app/register/page.tsx`
- `src/app/login/page.tsx`

### 로그인 처리

1. 현재 사용 중인 Auth.js beta의 Credentials 공급자가 `Request`를 전달하는 공식 시그니처를 먼저 확인한다.
2. `authorize` 초입에서 IP+이메일 rate limit을 검사한다.
3. 사용자가 없거나 비밀번호가 틀린 경우에도 동일한 일반 오류와 거의 같은 처리 흐름을 유지한다.
4. 성공 시 해당 이메일·IP의 실패 제한 상태를 초기화한다.
5. API 응답과 로그인 화면에서 `429`을 일반적인 “잠시 후 다시 시도” 메시지로 처리하고, `Retry-After`에 맞춰 제출 버튼을 비활성화한다.
6. 로그인 로그에는 비밀번호·전체 이메일·원본 IP를 기록하지 않는다.

### 회원가입 서버 검증

`POST /api/register`는 JSON 파싱 직후 아래를 확인한다.

| 필드 | 정책 |
| --- | --- |
| `name` | 문자열, trim 후 1–50자, 제어 문자 금지 |
| `email` | 정규화 후 254자 이하, 기본 이메일 형식, DB 조회·저장에 동일 정규화값 사용 |
| `password` | 문자열, 12–128자, 공백만으로 구성 불가. 실제 정책은 UI와 서버 모두 동일하게 적용 |
| `cohortId` | 문자열, 최대 64자, 활성 기수인지 DB에서 재확인 |
| 본문 | JSON `Content-Type` 확인 및 합리적 요청 본문 상한 적용 |

1. 가입을 생성하기 전 IP+이메일 제한을 확인한다.
2. 기존 이메일 존재 여부와 관계없이 외부 응답은 계정 존재를 과도하게 노출하지 않는 메시지로 통일하는 방안을 적용한다. 단, 로그인 UX와 충돌하지 않도록 화면 문구를 함께 조정한다.
3. Prisma unique 충돌은 예상 가능한 `409` 또는 일반 가입 실패로 처리하고 원시 DB 오류를 노출하지 않는다.
4. 가입 성공 후 자동 로그인은 추가하지 않는다.
5. 클라이언트 입력 요소에는 서버 상한과 일치하는 `maxLength`, 비밀번호 최소 길이를 설정하되, 보안 기준은 서버 검증으로 둔다.

### 테스트

- 틀린 비밀번호 반복, 존재하지 않는 이메일 반복, 맞는 비밀번호 로그인의 제한·초기화 경로
- 이메일 대소문자·공백 우회
- 이름·이메일·비밀번호·기수 ID·본문이 상한 또는 형식 위반일 때 `400`
- 같은 가입 이메일의 동시 요청에서 계정이 하나만 생성됨
- 정상 회원가입과 로그인 회귀

## Phase S3 — 이메일 계정 열거 제거

### 권장 정책

현재 시스템은 이메일 소유권을 증명하는 메일 발송 수단, 전화번호, 복구 코드가 없다. 따라서 이름만 받고 마스킹 이메일을 반환하는 기능은 본인 확인 기능이 아니며 유지하면 안 된다.

권장 구현:

1. 로그인 화면의 “이메일 찾기” 진입점과 모달을 제거한다.
2. `POST /api/find-email`을 제거하거나 항상 `404`로 종료한다.
3. 관련 사용자 안내는 “관리자에게 문의해 계정 정보를 확인하세요”로 바꾼다.

향후 이메일 서비스가 도입될 때만 다음의 별도 프로젝트로 복원한다.

- 사용자 입력 이메일 또는 사전 검증된 복구 수단으로만 안내를 전송
- 존재 여부와 무관한 동일 응답 및 처리 시간
- 짧은 만료의 일회성 토큰, 해시 저장, 발급 및 사용 rate limit
- 감사 로그와 개인정보 보존 기간 정책

### 테스트

- 비로그인 상태에서 이름 조합을 변경해도 이메일·계정 수·존재 여부가 응답에 포함되지 않는다.
- 이전 엔드포인트 URL은 개인정보를 반환하지 않는다.
- 로그인 화면에서 계정 식별 정보를 노출하는 UI가 남아 있지 않다.

## Phase S4 — 보안 응답 헤더

### 변경 대상

- `next.config.ts`
- 필요 시 Markdown·이미지 렌더링 컴포넌트 및 외부 Blob 도메인 목록

### 기본 헤더

`headers()`를 사용해 모든 애플리케이션 응답에 아래를 적용한다.

| 헤더 | 초기 정책 |
| --- | --- |
| `Content-Security-Policy` | 기본 출처는 self, 프레임은 차단, 객체는 차단, 폼 전송은 self, base URI는 self로 제한 |
| `X-Content-Type-Options` | `nosniff` |
| `Referrer-Policy` | `strict-origin-when-cross-origin` |
| `X-Frame-Options` | `DENY` |
| `Permissions-Policy` | camera·microphone·geolocation 등 미사용 기능 차단 |
| `Strict-Transport-Security` | production HTTPS에서만 적용 |

### CSP 설계 원칙

1. Next.js 16.3.5 개발·프로덕션 요구사항을 문서로 재확인한 뒤 적용한다.
2. `script-src`는 임의 외부 도메인을 열지 않으며, Next.js 런타임 요구사항을 최소 범위로만 허용한다.
3. `style-src`의 inline 스타일 필요 여부를 빌드 결과와 실제 화면에서 점검한다. 현재 Tailwind와 Next.js가 필요로 하는 범위보다 넓은 허용은 금지한다.
4. `img-src`에는 `self`, `https:`, Vercel Blob 실제 호스트를 명시한다. `data:` 이미지가 필요하지 않은 현재 정책은 유지한다.
5. `connect-src`, `font-src`는 실제 사용하는 self·Vercel·글꼴 출처만 허용한다.
6. 최초 배포는 Report-Only 모드로 CSP 위반을 수집·수정한 뒤 강제 모드로 전환한다.
7. 동적 nonce가 필요하다고 확인되는 경우 현재 `proxy.ts`와 Next.js 권장 구성에 맞춰 nonce를 생성하되, 임의의 `'unsafe-inline'`으로 CSP를 무력화하지 않는다.

### 테스트

- 주요 로그인·대시보드·공개 문서·이미지 업로드 화면에서 콘솔 CSP 위반이 없다.
- 외부 공유 문서의 허용된 Vercel Blob 이미지가 표시된다.
- 인라인 이벤트, `javascript:` URL, 프레임 삽입, MIME 스니핑이 헤더 정책으로도 차단된다.
- production HTTPS 응답에만 HSTS가 존재한다.

## Phase S5 — 프로젝트 입력 검증 통일

### 변경 대상

- `src/app/api/projects/route.ts`
- `src/app/api/projects/[projectId]/route.ts`
- 프로젝트 생성·수정 UI
- 필요 시 작업·일정·아카이브 API에 같은 검증 도구를 단계 확대 적용

### 프로젝트 정책

| 필드 | 정책 |
| --- | --- |
| `name` | trim 후 1–100자 |
| `description` | 문자열 또는 null, 최대 5,000자 |
| `summary` | 문자열 또는 null, 최대 300자 |
| `category` | 문자열 또는 null, 최대 50자 |
| `tags` | 최대 10개, 태그당 최대 30자, 중복 제거 후 저장 |
| `status` | 서버 허용 목록(`planning`, `active`, `paused`, `completed`)만 허용 |
| `color` | `#RRGGBB` 형식만 허용 |
| JSON 본문 | 콘텐츠 유형 검사 및 합리적인 상한 적용 |

1. 생성과 수정이 정확히 같은 검증 함수를 사용하도록 한다.
2. 부분 수정은 전달된 필드만 검사한다. 전달되지 않은 필드는 변경하지 않는다.
3. 문자열이 아닌 값, 객체·배열을 기대하지 않은 필드, 큰 본문은 DB 접근 전에 `400`으로 거부한다.
4. UI 입력의 `maxLength`, 색상 선택, 상태 선택지는 서버 허용 정책과 동일하게 맞춘다.
5. 아카이브 본문과 이미지에는 별도 정책을 유지한다. 이미지 4 MiB 제한은 현재 구현을 계속 사용하고, Markdown 본문은 별도 상한을 정해 후속 검증 범위에 포함한다.

### 테스트

- 생성·수정 모두 각 필드의 경계값을 허용한다.
- 한 글자 초과, 잘못된 색상·상태, 11개 태그, 비문자열 JSON 값은 `400`이다.
- 유효한 부분 PATCH가 다른 필드를 지우지 않는다.
- 소유자·`space_admin`·권한 없는 사용자의 기존 권한 매트릭스가 유지된다.

## 4. 실행 및 배포 순서

1. Phase S1 공통 모듈과 단위 테스트
2. Phase S2 로그인·가입 보호 및 회귀 테스트
3. Phase S3 공개 이메일 찾기 제거
4. Phase S4 CSP Report-Only 배포 및 위반 분석
5. CSP 강제 모드 전환
6. Phase S5 프로젝트 입력 검증 및 API·UI 테스트
7. `npm test`, `npx tsc --noEmit`, `npm run build`, `npm audit` 실행
8. Preview 환경에서 정상 로그인·가입·프로젝트 생성·수정·공개 문서·이미지 표시 수동 점검
9. Production 배포 후 24–72시간 동안 `429`, CSP 위반, `400`, `503` 비율을 모니터링

## 5. 운영 점검과 롤백

### 모니터링 지표

- 흐름별 요청 수, 허용/제한(`429`) 비율, rate-limit 저장소 오류(`503`) 비율
- 로그인 성공·실패 비율 및 급격한 IP/이메일 집중
- CSP Report-Only 위반의 directive·출처별 집계
- 가입 요청과 생성된 계정 수의 차이
- 검증 실패(`400`) 필드별 집계

개인정보 원문, 비밀번호, 세션 토큰, 전체 이메일, 원본 IP는 로그와 분석 이벤트에 기록하지 않는다.

### 롤백 기준

- 정상 사용자의 로그인·가입 `429` 비율이 지속적으로 높으면 우선 한도만 완화한다.
- Redis 장애가 발생하면 민감 엔드포인트를 fail closed로 유지하고, 운영 공지 및 저장소 복구를 우선한다. 보안을 위해 무제한 허용으로 즉시 전환하지 않는다.
- CSP 강제 모드가 정상 기능을 막으면 이전에 수집한 Report-Only 위반을 근거로 최소 출처만 추가하고 재배포한다. 전체 CSP를 제거하거나 광범위한 `'unsafe-inline'`을 추가하지 않는다.
- 입력 검증이 기존 유효 데이터를 막으면 허용 상한·열거형을 데이터 분석 후 조정하고, 서버 검증 자체를 우회하지 않는다.

## 6. 최종 체크리스트

- [ ] Vercel Production 환경에서 `AUTH_SECRET`, `NEXTAUTH_URL`, `AUTH_TRUST_HOST=true`가 설정됐다.
- [ ] Upstash Redis 환경 변수는 Preview·Production에 설정됐다.
- [ ] 로그인, 가입, 계정 조회가 IP+입력값 기준으로 제한된다.
- [ ] 공개 이메일 찾기 UI와 개인정보 응답이 제거됐다.
- [ ] 가입과 프로젝트 API가 공통 서버 검증을 사용한다.
- [ ] CSP가 Report-Only 검증 후 강제 모드로 전환됐다.
- [ ] 보안 헤더가 공개 페이지·API·대시보드에서 확인됐다.
- [ ] 모든 자동 테스트, 타입 검사, production build가 통과했다.
- [ ] `npm audit`의 남은 Prisma CLI 전이 의존성 항목은 위험 수용 사유와 추적 일정이 기록됐다.
