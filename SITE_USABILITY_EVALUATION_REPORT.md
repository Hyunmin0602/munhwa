# 사이트 전체 사용성 평가 리포트

작성일: 2026-09-19

## 1. 평가 목적과 범위

문화체육위원회 내부 업무 관리 시스템이 사업 생성부터 칸반, 일정, 아카이브, 통합 현황 확인까지의 업무 흐름을 효율적이고 안전하게 지원하는지 평가했다.

검토 범위는 다음과 같다.

- 인증: 로그인, 이메일 찾기
- 전역 탐색: 데스크톱 사이드바, 모바일 하단 탭, 사업 전환
- 사업 관리: 사업 생성, 수정, 태그 필터
- 업무 실행: 칸반 카드, 일정, 아카이브와 회의록
- 통합 화면: 다사업 현황, 아카이브 전체 보기, 필터
- 상태 설계: 로딩, 빈 상태, 오류, 저장 및 삭제 피드백
- 접근성: 키보드 조작, 버튼 레이블, 포커스와 모바일 터치 흐름

본 평가는 2026-09-19 기준의 코드 및 화면 구조 검토 결과다. 실제 사용자 관찰, 보조기술 사용자 테스트, 운영 데이터 분석은 포함하지 않았다.

## 2. 종합 평가

| 항목 | 평가 | 요약 |
| --- | --- | --- |
| 정보 구조와 탐색 | 양호 | 통합, 사업, 회의록의 진입점이 명확하고 모바일 사업 전환도 제공한다. |
| 핵심 업무 처리 | 보통 | 칸반, 일정, 아카이브의 주요 작업은 갖췄지만 실패 시 결과를 잘못 보여 주는 삭제 흐름이 있다. |
| 피드백과 복구 | 개선 필요 | 전역 토스트와 일부 재시도는 있으나 일정·아카이브·카드 상세에서 실패가 묵살되는 경우가 있다. |
| 데이터 탐색 | 양호 | 통합 화면의 유형 탭, 기간, 사업·태그 필터, 아카이브 전체 보기의 조합이 유용하다. |
| 모바일 사용성 | 양호 | 하단 탭, 사업 전환 시트, 일정 일간 보기, 칸반 스와이프 이동을 제공한다. |
| 접근성 | 개선 필요 | 일부 아이콘 버튼의 접근 가능한 이름이 없고, 칸반의 핵심 조작이 포인터 중심이다. |

현재 제품은 익숙한 내부 사용자에게는 빠른 업무 처리가 가능한 수준이다. 다만 네트워크가 불안정하거나 키보드 중심으로 사용하는 상황에서는 신뢰와 조작 가능성이 낮아진다. 다음 분기 개선의 최우선은 기능 추가보다 실패 상태를 정확히 반영하고 복구할 수 있게 만드는 것이다.

## 3. 주요 사용자 흐름 평가

### 3.1 로그인과 진입

로그인 폼은 이메일 자동완성, 비밀번호 표시 전환, 잘못된 자격 증명 메시지, 이메일 찾기를 제공한다. 회원가입 완료 후 안내도 있어 기본 진입 흐름은 자연스럽다.

개선점은 비밀번호 표시 전환 버튼이 키보드 탭 순서에서 제외되어 있다는 점이다. 키보드 사용자는 이 기능에 도달할 수 없고 버튼의 접근 가능한 이름도 없다. 로그인 실패 메시지는 적절하지만 네트워크 실패와 자격 증명 실패를 구분하지 않아 조치 방법이 모호하다.

### 3.2 전역 탐색과 사업 전환

데스크톱 사이드바는 통합 화면, 회의록, 사업 목록과 사업별 하위 메뉴를 한곳에 모아 탐색 구조가 이해하기 쉽다. 모바일은 하단 탭과 현재 사업 전환 시트를 별도로 제공하므로 다른 사업으로의 이동 비용이 낮다.

다만 사이드바의 일부 아이콘 버튼은 `title`에 의존하고 명시적인 `aria-label`이 없다. 화면 판독기에서는 기능을 충분히 설명하지 못할 수 있다. 사업이 많아질 때를 대비해 사이드바 사업 목록에 검색 또는 접기 기능도 필요하다.

### 3.3 사업 생성과 메타데이터

사업명, 상세 설명, 카테고리, 태그, 색상을 입력할 수 있고 상세 설명의 글자 수와 서버 검증 기준이 맞춰져 있다. 태그는 통합 화면의 필터와 연결되어 사업 분류 체계로 활용할 수 있다.

태그는 자유 입력 방식이므로 유사 태그가 분산될 가능성이 높다. 예를 들어 `교육`, `교육사업`, `교육 사업`이 각각 생성되면 통합 필터의 신뢰성이 떨어진다. 추천 태그, 기존 태그 자동완성, 관리자 표준 태그 정책 중 하나가 필요하다.

### 3.4 칸반 업무 관리

카드 빠른 추가, 상세 편집, 담당자·마감일·우선순위, 드래그 정렬, 모바일 스와이프 이동이 제공되어 핵심 업무 처리 밀도가 높다. 카드 상세는 저장되지 않은 변경을 표시하고 `Ctrl/Cmd+Enter` 저장을 지원한다.

그러나 카드 삭제와 컬럼 삭제는 요청 성공 여부를 확인하지 않은 뒤 화면 상태를 변경하는 경로가 있다. 삭제 요청이 실패하면 실제 서버에는 남아 있지만 사용자 화면에서는 사라질 수 있다. 또한 드래그와 카드 열기가 마우스/터치 중심이어서 키보드만으로는 카드 이동과 상세 열기가 어렵다.

### 3.5 일정

모바일은 날짜별 일간 보기와 빠른 일정 추가를 제공하고, 데스크톱은 월간 보기와 예정 일정 목록을 제공한다. 각 기기에서 주요 목적에 맞게 화면이 달라지는 점은 강점이다.

일정 조회·생성·삭제 실패가 사용자에게 드러나지 않는다. 특히 삭제는 요청 실패 후에도 로컬 목록에서 제거하므로 데이터가 사라진 것처럼 보이는 심각한 신뢰 문제를 만들 수 있다. 일정 삭제 전 확인과 실패 시 재시도도 필요하다.

### 3.6 아카이브와 회의록

문서 목록에는 미리보기, 갱신 시각, 공개 범위, 작성자가 표시된다. 편집기는 마크다운 도구 모음, 미리보기, 저장 단축키, 공개 및 공유 링크 제어를 제공해 문서 작업 흐름이 충실하다.

목록 조회·생성·삭제 및 편집기 로드·저장 실패가 대부분 무음으로 처리된다. 네트워크 오류가 발생하면 빈 목록 또는 이전 편집 상태만 보여 사용자가 원인을 알기 어렵다. 삭제 역시 실패 후 항목을 화면에서 제거할 수 있다. 문서 편집에는 저장 중·저장됨 외에도 실패 상태와 마지막 저장 시각을 표시해야 한다.

### 3.7 통합 화면과 태그 필터

통합 화면은 최근 업데이트, 칸반 현황, 다가오는 일정, 아카이브를 분리해 요약하고 각 섹션에서 전체 목록으로 연결한다. 태그 필터는 선택한 태그를 가진 사업만 대상으로 모든 유형을 함께 좁히며 URL에도 보존되어 공유와 재방문에 유리하다.

태그 검색창은 입력값을 그대로 적용할 수 있어 존재하지 않는 태그 입력 시 빈 결과가 나온다. 선택 가능한 태그만 적용하거나, 일치하는 태그가 없을 때 명확한 안내와 초기화 행동을 제공해야 한다. 사업 필터와 태그 필터를 동시에 사용했을 때 현재 적용 조건을 헤더에 칩으로 표시하면 결과 범위를 더 쉽게 이해할 수 있다.

## 4. 발견 사항과 우선순위

| 우선순위 | 발견 사항 | 사용자 영향 | 권장 조치 |
| --- | --- | --- | --- |
| P0 | 삭제 API 실패 후에도 카드·일정·문서를 화면에서 제거할 수 있음 | 데이터가 삭제된 것으로 오인하고 같은 내용을 중복 작성하거나 업무 판단을 잘못할 수 있음 | `res.ok` 확인 후에만 로컬 상태를 변경하고, 실패 시 오류 메시지와 재시도를 제공한다. |
| P0 | 일정·아카이브의 조회·생성·저장 실패가 무음 처리됨 | 빈 상태와 오류를 구별하지 못하고 업무가 누락될 수 있음 | 컴포넌트별 오류 상태, 재시도 버튼, 작업 중 중복 방지, 성공/실패 토스트를 일관되게 제공한다. |
| P1 | 칸반의 핵심 조작이 포인터 중심이며 키보드 대안이 부족함 | 키보드 및 일부 보조기술 사용자가 카드 이동·상세 열기를 수행하기 어려움 | `KeyboardSensor`를 추가하고 카드 상세 열기 버튼 및 이동 메뉴를 제공한다. |
| P1 | 모달이 포커스를 가두거나 복귀시키지 않음 | 키보드 사용자가 모달 뒤 요소로 이동하거나 닫은 뒤 위치를 잃을 수 있음 | 공통 다이얼로그 컴포넌트에 포커스 트랩, `role="dialog"`, `aria-modal`, 최초·복귀 포커스를 구현한다. |
| P1 | 태그가 자유 입력이라 분류가 쉽게 분산됨 | 태그 기반 사업 탐색의 재현성과 완전성이 낮아짐 | 기존 태그 자동완성, 선택형 태그, 유사 태그 병합 정책을 도입한다. |
| P2 | 필터 적용 조건이 결과 화면에서 충분히 드러나지 않음 | 사용자가 왜 항목이 보이지 않는지 파악하기 어려움 | 헤더에 사업 수·선택 태그 칩과 개별 해제 버튼을 표시한다. |
| P2 | 긴 사업 목록의 탐색 도구가 부족함 | 사업 수 증가 시 사이드바와 모바일 전환 시트의 탐색 시간이 증가함 | 사업명 검색, 최근 사용, 고정 사업을 제공한다. |
| P2 | 위험 작업의 복구 수단이 없음 | 실수로 삭제한 카드·일정·문서를 되돌릴 수 없음 | 삭제 후 5~10초 실행 취소 또는 소프트 삭제와 휴지통을 제공한다. |

## 5. 권장 실행 계획

### 1단계: 신뢰성 회복

1. 카드·일정·문서의 삭제, 생성, 저장 요청에 성공 여부 확인을 추가한다.
2. 실패 시 해당 화면에 오류 메시지와 재시도 동작을 표시한다.
3. 낙관적 업데이트가 필요한 경우 이전 상태를 보관해 실패 시 즉시 되돌린다.
4. 삭제 확인 문구에 대상 이름과 되돌릴 수 없음을 명시한다.

### 2단계: 접근성과 탐색성 강화

1. 모든 아이콘 전용 버튼에 `aria-label`을 추가한다.
2. 모달 공통 접근성 처리를 적용한다.
3. 칸반 카드 이동의 키보드 대안과 카드 열기 버튼을 제공한다.
4. 사이드바와 모바일 사업 전환 시트에 사업 검색을 추가한다.

### 3단계: 정보 검색 정교화

1. 태그 선택형 입력과 표준화 규칙을 도입한다.
2. 통합 화면에 활성 필터 칩과 결과 개수를 표시한다.
3. 아카이브에 제목·내용 검색, 공개 범위·작성자·기간 필터를 추가한다.
4. 자주 쓰는 통합 필터 조합을 저장할 수 있게 한다.

## 6. 수용 기준과 검증 방법

P0 개선 완료 전에는 다음 시나리오가 모두 통과해야 한다.

1. 오프라인 또는 API 500 응답에서 카드·일정·문서 삭제를 시도해도 목록이 변하지 않고 오류 및 재시도가 보인다.
2. 일정·아카이브 조회 실패가 빈 상태가 아닌 오류 상태로 구분된다.
3. 생성·저장 버튼은 요청 중 중복 실행되지 않는다.
4. 태그 필터 적용 후 URL 새로고침에도 같은 사업 범위와 결과가 유지된다.

접근성은 키보드만으로 로그인, 사업 생성, 카드 상세 열기·저장·닫기, 일정 추가, 문서 저장, 통합 태그 필터 적용을 완료할 수 있어야 한다. 화면 검증은 최소 360x800, 390x844, 768x1024, 1440x900 뷰포트에서 수행한다.

## 7. 결론

제품의 정보 구조와 모바일 업무 흐름은 내부 업무 시스템에 적합하게 설계되어 있다. 다음 개선의 핵심은 화면을 더 복잡하게 만드는 것이 아니라, 작업 성공·실패를 정확히 알리고 실패해도 사용자가 데이터와 맥락을 잃지 않게 하는 것이다. P0 항목을 먼저 해결한 후 키보드 접근성과 태그 표준화를 진행하는 순서를 권장한다.