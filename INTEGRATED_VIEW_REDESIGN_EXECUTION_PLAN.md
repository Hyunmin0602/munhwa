# 통합 화면 재디자인 실행 계획

## 1. 목적

통합 화면을 모든 데이터를 단순히 나열하는 곳이 아니라, 칸반·일정·문서의 고유한 정보 형태를 유지하면서 전체 사업의 상태를 한눈에 파악하는 작업 허브로 정리한다. 사용자는 이 화면에서 흐름을 판단하고, 필요한 항목만 원래 사업 화면으로 이동해 상세 작업한다.

이번 계획은 현재 구현된 `통합`, `사업`, `더보기` 모바일 내비게이션을 기준으로 한다. 모바일과 태블릿의 기본 진입 화면은 통합 화면이며, 사업 탭은 참여 가능한 사업 목록을 보여 준다.

## 2. 현재 기준선

- `/dashboard/integrated`는 작업, 일정, 문서, 회의록을 단일 목록으로 조회한다.
- 모바일과 태블릿에서 `/dashboard` 접근 시 통합 화면으로 이동한다.
- 모바일 하단 탭은 `통합`, `사업`, `더보기` 세 항목이다.
- 사업 목록의 카드 선택은 해당 사업 칸반 보드로 이동한다.
- 관리자 계정은 모든 사업 목록과 칸반 데이터를 조회하고 관리할 수 있어야 한다.
- 칸반 API 실패는 빈 보드가 아니라 재시도 가능한 오류 상태로 보여야 한다.

## 3. UX 원칙

1. 칸반은 진행 단계, 일정은 시간, 문서는 최신성이라는 원래 맥락을 보존한다.
2. 한 화면에서 각 유형의 핵심 상태를 모두 볼 수 있게 하되, 항목 수는 요약 범위로 제한한다.
3. 모든 항목은 사업 색상, 사업명, 날짜를 항상 표시한다.
4. 통합 화면에서는 빠른 상태 파악과 이동만 제공하고, 상세 편집은 원래 사업 화면에서 한다.
5. 모바일에서 제목, 사업명, 날짜를 우선 보존하고 부가 정보는 공간이 부족할 때 감춘다.
6. 필터의 임시 선택은 적용 전까지 URL과 목록을 바꾸지 않는다.

## 4. 목표 정보 구조

### 재검토 결정

- **사업별 칸반 의미를 보존한다.** 사업마다 컬럼명과 흐름이 다르므로 전체 사업의 컬럼을 `할 일·진행 중·완료`로 합치지 않는다. 칸반은 사업명을 헤더로 둔 미니 보드로 표시하고 각 사업의 실제 컬럼명과 순서를 그대로 사용한다.
- **전체 보기와 집중 보기를 구분한다.** `전체 보기`만 칸반·일정·문서를 모두 보여 준다. `칸반`·`일정`·`문서`·`회의록` 탭은 선택 유형의 상세 목록을 보여 주고, 다른 유형은 상단 요약 수치로만 남긴다. 탭을 눌렀는데 다른 목록이 계속 길게 보이는 혼동을 막는다.
- **숨겨진 데이터 수를 표시한다.** 요약에 포함하지 않은 항목은 `전체 n개`로 표시한다. 사용자는 화면에 보이는 소수의 항목이 전체 데이터라고 오해하지 않는다.
- **색상은 보조 신호로만 쓴다.** 사업 색상 점과 함께 사업명을 항상 표기하고, 우선순위·문서 공개 범위·일정 상태는 아이콘과 텍스트를 함께 표시한다.

```text
통합 화면                                      [필터] [새로고침]
[전체 보기] [칸반] [일정] [문서] [회의록]
[오늘] [7일] [30일] [전체]

칸반 현황                                      전체 보기 >
[겨울 MT]  [준비] 장소 대관 · 장보기  [진행] 예산안 검토
[문화제]   [섭외] 연사 확정           [완료] 결과 보고서
전체 5개 사업 · 작업 18개

다가오는 일정                                  전체 보기 >
오늘 14:00  운영 회의                 문화제 사업
8/18        장소 답사                 겨울 MT
전체 7개 일정

최근 문서 · 회의록                             전체 보기 >
회의록: 8월 운영 회의               오늘 · 문화제 사업
문서:   행사 운영안                  어제 · 겨울 MT
전체 12개 문서
```

- 기본 탭은 `전체 보기`이며, 칸반·일정·문서 섹션을 같은 화면에 모두 표시한다.
- `칸반` 섹션은 사업별 보드 전체를 복제하지 않는다. 우선순위가 높거나 마감이 임박한 사업 최대 2개의 미니 보드를 보여 주며, 각 보드는 실제 컬럼 순서대로 최대 3개 컬럼과 컬럼당 최대 2개 작업을 표시한다.
- 미니 보드에 표시되지 않은 사업과 작업은 `전체 n개` 링크에서 확인한다. 사업 필터를 하나만 선택하면 해당 사업의 미니 보드를 우선 표시한다.
- `일정` 섹션은 가장 가까운 날짜순으로 최대 5개를 시간 축 또는 날짜 묶음으로 표시한다. 종료된 일정은 기본 범위에서 제외한다.
- `문서` 섹션은 문서와 회의록을 최신 수정 순으로 최대 5개 보여 주고, 유형·작성자·수정일·공개 범위를 표시한다.
- 유형 탭을 선택하면 선택 유형은 확장 목록으로 전환하고, 상단 컨텍스트 띠에는 나머지 유형의 건수만 유지한다.
- 각 섹션의 `전체 보기`와 각 항목은 해당 사업의 칸반, 일정, 아카이브 상세로 이동한다.

## 5. 화면별 계획

### 모바일: 360-767px

- 상단은 페이지명과 필터 아이콘, 새로고침 아이콘으로 구성한다.
- 유형 탭과 기간 선택은 가로 스크롤 가능한 고정 높이 제어로 유지한다.
- 사업 필터는 하단 시트에서 다중 선택하고 `적용`으로 확정한다.
- 전체 보기에서는 칸반, 일정, 문서를 세로 섹션으로 모두 보여 주되, 칸반은 사업별 미니 보드 최대 2개, 일정과 문서는 최대 3개 항목과 `전체 보기` 링크를 제공한다.
- 칸반 스냅샷은 사업별 가로 스크롤 레일로 표시한다. 미니 보드는 실제 컬럼명·작업 수·작업 제목을 유지하며, 한 보드의 폭과 컬럼 폭을 고정해 내용 길이에 따라 흔들리지 않게 한다.
- 일정은 날짜 배지와 시간, 제목, 사업명을 한 행에 표시하며 최소 64px 높이를 유지한다.
- 문서는 문서/회의록 아이콘, 제목, 사업명, 수정일을 보여 주는 밀도 높은 행 목록을 사용한다.
- 긴 제목은 한 줄 말줄임 처리하고, 날짜는 상대 시간만 쓰지 않고 `8월 18일`처럼 절대 날짜를 함께 표시한다.
- 마지막 섹션이 하단 탭에 가려지지 않도록 본문 하단 여백을 둔다.
- 하단 `사업` 탭은 사업 목록으로 이동하며, 사업 카드를 누르면 칸반으로 진입한다.

### 태블릿: 768-1023px

- 모바일과 같은 기본 진입과 하단 탭 구조를 유지한다.
- 콘텐츠는 `max-w-6xl` 안에서 넓은 여백과 한 줄 필터 바를 사용한다.
- 사업 필터는 팝오버 또는 하단 시트 중 뷰포트 높이에 안전한 방식을 선택한다.
- 칸반은 사업별 미니 보드를 최대 2열로 보여 주고, 각 미니 보드 안의 실제 컬럼명은 가로 스크롤 없이 읽을 수 있도록 최대 3개만 노출한다. 일정과 문서는 각각 단일 열 목록으로 보여 준다.

### 데스크톱: 1024px 이상

- 기존 전체 사이드바와 사업별 상세 화면을 유지한다.
- 통합 화면의 상단에는 유형, 기간, 사업 필터와 새로고침을 인라인으로 배치한다.
- 넓은 화면에서는 칸반을 전체 너비 사업별 미니 보드 스트립으로 두고, 그 아래 일정과 문서를 2열로 나란히 둔다. 각 영역은 중첩 카드가 아닌 섹션 제목과 행 목록을 사용한다.

## 6. 구현 단계

| 단계 | 작업 | 완료 조건 |
| --- | --- | --- |
| 1 | 통합 API를 사업별 칸반·일정·문서 섹션 응답으로 확장 | 실제 컬럼명·순서·작업 수, 유형별 전체 수, 전체 보기 커서를 함께 반환 |
| 2 | 관리자 통합 조회 권한을 사업 목록 API와 일치시킨다 | 관리자는 전체 사업의 요약을 보고, 일반 사용자는 참여 사업 데이터만 받음 |
| 3 | `IntegratedHeader`, `IntegratedContextRail`, `IntegratedKanbanSnapshot`, `IntegratedScheduleList`, `IntegratedDocumentList`로 화면 분리 | 페이지 파일은 URL 상태와 데이터 요청만 담당 |
| 4 | 전체 보기에서 3개 유형 섹션을 구현 | 사업별 칸반 스냅샷·다가오는 일정·최근 문서가 한 화면에 함께 표시 |
| 5 | 유형 탭의 집중 보기와 섹션별 전체 보기 이동을 구현 | 선택 유형은 상세 목록, 다른 유형은 컨텍스트 띠의 건수만 유지 |
| 6 | 모바일 필터 시트와 태블릿 필터 제어 개선 | 적용 전 선택은 목록과 URL에 영향 없음 |
| 7 | 로딩, 빈 상태, 오류, 더 불러오기 상태를 유형별 고정 높이로 정리 | 실패가 빈 결과로 보이지 않음 |
| 8 | 기준 뷰포트 시각 검증 | 360x800, 768x1024, 1024x768, 1440x900에서 3개 유형이 겹치거나 잘리지 않음 |

## 7. 데이터와 권한 규칙

- 통합 API와 사업 목록 API는 일반 사용자에게는 참여 사업만 반환한다.
- `role = admin`은 전체 사업 목록과 칸반 상세 데이터를 조회하고 관리할 수 있다.
- 칸반 상세, 컬럼, 작업 API는 모두 같은 `관리자 또는 사업 멤버` 권한 조건을 사용한다.
- 권한 오류, 서버 오류, 네트워크 오류는 각각 정상 빈 상태와 구분해 표시한다.

## 8. 검증 기준

- 관리자 계정에서 다른 멤버가 소유한 사업의 칸반 컬럼과 작업이 보인다.
- 일반 사용자는 자신이 참여하지 않은 사업의 칸반 API에서 `403`을 받는다.
- 칸반 상세 요청 실패 시 오류 문구와 재시도 버튼이 보인다.
- 모바일과 태블릿에서 `/dashboard`가 통합 화면으로 이동한다.
- 모바일 사업 탭에서 사업 목록을 보고 선택한 사업의 칸반으로 이동한다.
- 전체 보기에서 칸반 단계, 다가오는 일정, 최근 문서·회의록을 스크롤 한 번으로 모두 확인할 수 있다.
- 사업별 서로 다른 칸반 컬럼명과 순서가 미니 보드에 그대로 표시되며, 다른 사업의 컬럼과 섞이지 않는다.
- 칸반 작업은 단계명·사업명·마감일을, 일정은 날짜·시간·사업명을, 문서는 유형·수정일·사업명을 유지한다.
- 유형 탭을 선택하면 선택 유형의 상세 목록과 다른 유형의 건수 요약을 명확히 구분해 표시한다.
- 요약 밖 항목 수와 `전체 n개` 이동 경로가 보이며, 긴 제목·사업명·날짜가 기준 뷰포트에서 잘리지 않는다.
- 필터, 빈 상태, 긴 사업명, 하단 안전 영역이 기준 뷰포트에서 겹치지 않는다.

## 9. 검증 명령

```bash
npx eslint src/app/dashboard/integrated/page.tsx src/components/KanbanBoard.tsx
npx tsc --noEmit
npm run build
git diff --check
```
