# 통합 화면 재디자인 실행 계획

## 1. 목적

통합 화면을 모든 데이터를 단순히 나열하는 곳이 아니라, 사용자가 지금 확인하고 처리할 업무를 빠르게 판단한 뒤 해당 사업 화면으로 이동하는 작업 허브로 정리한다.

이번 계획은 현재 구현된 `통합`, `사업`, `더보기` 모바일 내비게이션을 기준으로 한다. 모바일과 태블릿의 기본 진입 화면은 통합 화면이며, 사업 탭은 참여 가능한 사업 목록을 보여 준다.

## 2. 현재 기준선

- `/dashboard/integrated`는 작업, 일정, 문서, 회의록을 단일 목록으로 조회한다.
- 모바일과 태블릿에서 `/dashboard` 접근 시 통합 화면으로 이동한다.
- 모바일 하단 탭은 `통합`, `사업`, `더보기` 세 항목이다.
- 사업 목록의 카드 선택은 해당 사업 칸반 보드로 이동한다.
- 관리자 계정은 모든 사업 목록과 칸반 데이터를 조회하고 관리할 수 있어야 한다.
- 칸반 API 실패는 빈 보드가 아니라 재시도 가능한 오류 상태로 보여야 한다.

## 3. UX 원칙

1. 시간과 마감 정보를 사업 구분보다 먼저 표시한다.
2. 모든 통합 항목은 사업 색상, 사업명, 날짜를 항상 표시한다.
3. 통합 화면에서는 편집하지 않고 원래 사업 화면으로 이동한다.
4. 모바일에서 제목, 사업명, 날짜를 우선 보존하고 부가 정보는 공간이 부족할 때 감춘다.
5. 필터의 임시 선택은 적용 전까지 URL과 목록을 바꾸지 않는다.

## 4. 목표 정보 구조

```text
통합 화면                                      [필터] [새로고침]
[전체] [작업] [일정] [문서] [회의록]
[오늘] [7일] [30일] [전체]

오늘과 임박한 일정
  일정 행

마감이 가까운 작업
  작업 행

최근 문서와 회의록
  문서 행
```

- 기본 탭은 `전체`이며, 시간 순으로 섞는 기존 API 결과를 유지한다.
- 화면에는 시간 성격에 맞는 섹션 제목을 표시하되, 필터를 선택하면 해당 유형의 단일 목록으로 단순화한다.
- 각 행 전체를 클릭하면 작업은 칸반, 일정은 일정, 문서와 회의록은 아카이브 상세로 이동한다.

## 5. 화면별 계획

### 모바일: 360-767px

- 상단은 페이지명과 필터 아이콘, 새로고침 아이콘으로 구성한다.
- 유형 탭과 기간 선택은 가로 스크롤 가능한 고정 높이 제어로 유지한다.
- 사업 필터는 하단 시트에서 다중 선택하고 `적용`으로 확정한다.
- 통합 행은 최소 72px 높이로 두고, 마지막 항목이 하단 탭에 가려지지 않도록 본문 하단 여백을 둔다.
- 하단 `사업` 탭은 사업 목록으로 이동하며, 사업 카드를 누르면 칸반으로 진입한다.

### 태블릿: 768-1023px

- 모바일과 같은 기본 진입과 하단 탭 구조를 유지한다.
- 콘텐츠는 `max-w-6xl` 안에서 넓은 여백과 한 줄 필터 바를 사용한다.
- 사업 필터는 팝오버 또는 하단 시트 중 뷰포트 높이에 안전한 방식을 선택한다.
- 목록은 단일 열을 유지해 시간 흐름을 분리하지 않는다.

### 데스크톱: 1024px 이상

- 기존 전체 사이드바와 사업별 상세 화면을 유지한다.
- 통합 화면의 상단에는 유형, 기간, 사업 필터와 새로고침을 인라인으로 배치한다.
- 넓은 화면에서는 일정/작업과 문서/회의록을 보조 2열로 나눌 수 있으나, 각 열은 중첩 카드가 아닌 행 목록을 사용한다.

## 6. 구현 단계

| 단계 | 작업 | 완료 조건 |
| --- | --- | --- |
| 1 | 통합 API 응답에 유형별 요약 수를 추가 | 일정, 마감 임박 작업, 최근 문서 수를 한 요청에서 반환 |
| 2 | `IntegratedHeader`, `IntegratedSummaryRail`, `IntegratedTimeline`, `IntegratedItemRow`로 화면 분리 | 페이지 파일은 URL 상태와 데이터 요청만 담당 |
| 3 | 시간 성격별 섹션과 단일 유형 목록을 구현 | 전체/유형 탭 모두 원래 상세 화면으로 정상 이동 |
| 4 | 모바일 필터 시트와 태블릿 필터 제어 개선 | 적용 전 선택은 목록과 URL에 영향 없음 |
| 5 | 로딩, 빈 상태, 오류, 더 불러오기 상태를 공통 행 높이로 정리 | 실패가 빈 결과로 보이지 않음 |
| 6 | 기준 뷰포트 시각 검증 | 360x800, 768x1024, 1024x768, 1440x900에서 겹침과 잘림 없음 |

## 7. 데이터와 권한 규칙

- 통합 API와 사업 목록 API는 일반 사용자에게는 참여 사업만 반환한다.
- `role = admin`은 전체 사업 목록과 칸반 상세 데이터를 조회하고 관리할 수 있다.
- 칸반 상세, 컬럼, 작업 API는 모두 같은 `관리자 또는 사업 멤버` 권한 조건을 사용한다.
- 권한 오류, 서버 오류, 네트워크 오류는 각각 정상 빈 상태와 구분해 표시한다.

## 8. 검증 기준

- 관리자 계정에서 다른 멤버가 소유한 사업의 칸반 컬럼과 작업이 보인다.
- 일반 사용자는 자신이 참여하지 않은 사업의 칸반 API에서 `403`을 받는다.
- 칸반 상세 요청 실패 시 오류 문구와 재시도 버튼이 보인다.
- 모바일과 태블릿에서 `/dashboard`가 통합 화면으로 이동한다.
- 모바일 사업 탭에서 사업 목록을 보고 선택한 사업의 칸반으로 이동한다.
- 필터, 빈 상태, 긴 사업명, 하단 안전 영역이 기준 뷰포트에서 겹치지 않는다.

## 9. 검증 명령

```bash
npx eslint src/app/dashboard/integrated/page.tsx src/components/KanbanBoard.tsx
npx tsc --noEmit
npm run build
git diff --check
```
