# 문화체육위원회 업무 시스템 전체 구축 계획서

작성일: 2026-09-19

## 1. 목적과 범위

이 문서는 사업별 칸반, 일정, 아카이브, 회의록을 운영하는 현재 시스템을 안정적인 다중 사용자 업무 플랫폼으로 완성하기 위한 실행 계획이다. 기능 추가뿐 아니라 권한, 데이터 신뢰성, 오류 표시, 접근성, 운영 검증을 같은 완료 기준으로 관리한다.

### 목표

- 사용자가 사업 생성부터 업무 관리, 일정, 문서와 회의록 공유까지 끊김 없이 수행한다.
- 모든 업무 데이터는 공유 외부 libSQL/Turso 데이터베이스에서 관리한다.
- 서버 권한 판단과 화면 노출 범위를 일치시킨다.
- 네트워크·서버 실패가 성공처럼 보이지 않고, 사용자가 복구 행동을 알 수 있다.
- API와 UI의 공통 규칙을 정해 후속 기능이 같은 방식으로 확장된다.

### 비범위

- 사용자 본인이 관리자 권한을 승격하는 기능
- 로컬 SQLite를 운영 데이터 저장소로 지원하는 기능
- 외부 공유 링크에 비밀번호 또는 만료일을 강제하는 기능
- 검증되지 않은 범용 CRUD 추상화

## 2. 현재 기반과 완료 항목

| 영역 | 현재 상태 | 유지 원칙 |
| --- | --- | --- |
| 데이터 저장 | Prisma 7과 외부 libSQL/Turso 설정 사용 | 환경 변수가 없으면 시작하지 않으며 로컬 DB 폴백을 만들지 않음 |
| 사업 관리 | 사업명, 상세 설명, 색상, 태그 생성·수정 지원 | 서버에서 길이, 공백, 태그를 정규화 |
| 통합 화면 | 전체 사업의 칸반·일정·아카이브·회의록 집계와 태그 필터 지원 | 필터는 URL과 서버 조회 조건에 함께 반영 |
| 삭제 신뢰성 | 카드, 일정, 아카이브에 취소 가능한 지연 삭제와 실패 복구 적용 | 성공이 확인되기 전 영구 삭제로 보이지 않게 함 |
| 모달 오류 | 주요 생성·수정 모달에 `ModalErrorAlert` 적용 | 오류는 발생한 모달 내부에 표시하고 입력값을 유지 |
| 접근 제어 | 사업 멤버/소유자 기반 API 검증 존재 | API가 화면보다 먼저 권한을 보장 |

## 3. 목표 운영 모델

### 사용자와 권한

| 역할 | 기본 권한 | 추가 권한 |
| --- | --- | --- |
| 비로그인 사용자 | 외부 공유 문서 읽기 | 편집·내부 정보 접근 불가 |
| 일반 가입 사용자 | 공개 사업 요약 열람 | 초대 수락 후 해당 사업 상세 열람·작업 |
| 사업 참여자 | 칸반, 일정, 내부 아카이브 열람·편집 | 사업별 규칙 내 작업 수행 |
| 사업 소유자 | 참여자 관리, 초대, 공유 방식 관리, 사업 삭제 | 사업 메타데이터 정책은 변경 불가 |
| 관리자 | 기수, 카테고리, 태그 관리 | 전체 사업 접근 범위는 공통 권한 정책으로 명시 |

### 데이터 공개 범위

| 대상 | 비참여자 | 수락된 참여자 | 작성자 | 외부 링크 사용자 |
| --- | --- | --- | --- | --- |
| 사업 요약 | 공유 방식에 따라 열람 | 열람 | 열람 | 불가 |
| 칸반·일정 | 불가 | 열람·편집 | 열람·편집 | 불가 |
| 비공개 아카이브 | 불가 | 불가 | 열람·편집 | 불가 |
| 내부 아카이브 | 불가 | 열람·편집 | 열람·편집 | 불가 |
| 외부 아카이브 | 불가 | 열람·편집 | 열람·편집 | 읽기 전용 |

## 4. 구축 원칙

1. 권한은 클라이언트가 아닌 API와 DB 조회로 최종 판단한다.
2. 변경 요청은 응답 성공을 확인한 뒤 확정하고, 낙관적 UI에는 반드시 복구 경로를 둔다.
3. 자동 재시도는 안전한 읽기 요청에만 기본 적용한다. 생성·수정·삭제는 명시적 멱등성 보장 없이 자동 재시도하지 않는다.
4. API 오류는 영문 내부 문구가 아닌 코드와 한국어 사용자 문구를 포함한 하나의 계약으로 반환한다.
5. 같은 유형의 화면은 같은 로딩, 빈 상태, 오류, 재시도, 확인 대화상자 패턴을 사용한다.
6. 기능 단위마다 권한, 실패, 모바일 화면, 타입 검증을 함께 완료한다.

## 5. 단계별 구축 계획

### 단계 0. 운영 기반 정리

**목적:** 외부 DB와 배포 환경을 안정적으로 운영할 기반을 확정한다.

- `TURSO_DATABASE_URL`과 `TURSO_AUTH_TOKEN`을 배포 환경에 설정한다.
- 외부 DB에 `npx prisma migrate deploy`를 적용한다.
- 기존 `prisma/dev.db` 데이터가 필요하면 별도 이전 절차와 검증 목록을 작성한 뒤 이전한다.
- 개발·스테이징·운영 환경별 DB를 분리하고, 자격 증명은 저장소에 커밋하지 않는다.
- 백업, 마이그레이션 적용자, 장애 시 복구 절차를 운영 문서로 정한다.

**완료 조건:** 환경 변수 누락 시 명확히 실패하며, 외부 DB에서 로그인·사업 조회·생성·수정·삭제가 동작한다.

### 단계 1. 요청 신뢰성과 칸반 정합성

**목적:** 중복 생성과 실패한 변경의 UI 불일치를 제거한다.

- `apiFetch`를 읽기와 변경 요청 정책으로 분리한다.
  - `GET`/`HEAD`는 제한된 자동 재시도를 허용한다.
  - `POST`/`PATCH`/`DELETE`는 기본 재시도 횟수를 0으로 한다.
  - 화면별 오류 처리가 있을 때 전역 토스트를 중복 표시하지 않는 옵션을 제공한다.
- 칸반의 컬럼 이름 변경, 컬럼 삭제, 카드 이동, 순서 변경, 카드 생성에 성공 응답 확인과 실패 복구를 추가한다.
- 삭제는 공통 확인 대화상자와 취소 가능한 삭제 흐름으로 통일한다.
- 일정·아카이브·카드 삭제의 기존 undo 동작을 회귀 검증한다.

**완료 조건:** 네트워크 단절, 4xx, 5xx에서 생성 중복이 없고, 실패한 칸반 변경은 이전 상태로 복구되며 원인이 표시된다.

### 단계 2. API 인증·권한·오류 계약 표준화

**목적:** 라우트별로 달라진 접근 판정과 오류 형식을 제거한다.

- `api-guard.ts`와 `server-utils.ts`의 중복을 하나의 서버 권한 모듈로 통합한다.
- `requireUser`, `requireProjectAccess`, `requireProjectOwner`, `requireAdmin`을 정의한다.
- 관리자에게 허용하는 조회·수정·소유자 관리 범위를 정책으로 확정하고 모든 API에 동일 적용한다.
- 표준 오류 응답을 `{ error: { code, message } }`로 정의한다.
  - 예: `UNAUTHORIZED`, `FORBIDDEN`, `NOT_FOUND`, `VALIDATION_ERROR`, `CONFLICT`, `INTERNAL_ERROR`
- 각 라우트의 지역 `logApiError`를 공통 로거로 대체하고, 로그에는 범위·작업·식별 가능한 요청 맥락을 포함한다.
- 첫 적용 범위는 프로젝트 하위 API 한 묶음(컬럼, 카드, 일정)으로 제한하고 회귀 검증 뒤 아카이브·멤버·초대 API로 확장한다.

**완료 조건:** 동일한 인증·권한 실패가 모든 대상 API에서 같은 상태 코드와 오류 코드를 반환하며, 화면은 영문 내부 오류를 노출하지 않는다.

### 단계 3. 사업 공유, 초대, 메타데이터

**목적:** 사업 공개 범위와 참여자 수명주기를 명확히 한다.

- `Cohort`, 사업 공개 방식, 초대 상태, 프로젝트 요약 필드를 Prisma 스키마에 반영한다.
- 사업은 `PUBLIC` 또는 `INVITE_ONLY` 공유 방식을 갖고, 초대는 7일 후 만료되게 한다.
- 초대 수락과 `ProjectMember` 생성은 트랜잭션으로 처리하고 중복 수락을 방지한다.
- 비참여자용 요약 API와 참여자용 상세 API를 분리한다.
- 관리자 전용 기수·카테고리·태그 관리 API와 화면을 제공한다.
- 사업 목록 순서 변경은 전체 순서를 한 번에 전송하고 트랜잭션으로 저장한다.

**완료 조건:** 초대 대기자·만료 초대·거절 후 재초대·중복 초대가 일관되게 처리되며, 비참여자 응답에는 내부 업무 데이터가 포함되지 않는다.

### 단계 4. 아카이브와 회의록 완성

**목적:** 문서 협업과 외부 읽기 전용 공유를 안전하게 제공한다.

- 아카이브 공개 범위를 `PRIVATE`, `INTERNAL`, `EXTERNAL`로 관리한다.
- 외부 공유는 예측 불가능한 토큰과 활성 상태로 제어하며, 재생성·폐기를 지원한다.
- `canReadArchive`, `canEditArchive`를 분리해 공개 범위별 읽기·편집 권한을 강제한다.
- 외부 공유 API는 읽기 전용이며 편집 API와 경로를 분리한다.
- 게시물 `kind`로 일반 문서와 회의록을 구분하고, 회의일·참석자·안건을 단계적으로 확장한다.
- `ArchiveEditor`에 페이지 오류 상태, 저장 상태, 권한 오류, 재시도 동작을 추가한다.

**완료 조건:** 링크를 가진 비로그인 사용자는 외부 문서만 읽을 수 있고, 링크 폐기 직후 접근할 수 없으며, 저장 실패가 무음으로 끝나지 않는다.

### 단계 5. UI 상태·타입·대화상자 공통화

**목적:** 화면별 구현 편차를 줄이고 접근성과 유지보수성을 높인다.

- `Project`, `Task`, `Event`, `Member`, `Post` DTO를 도메인 타입 모듈로 통합한다.
- 목록 조회에 공통으로 쓰는 로딩, 오류, 빈 상태, 재시도 UI를 정리한다.
- 범용 훅은 API 계약이 확정된 뒤에 도입하며, 무분별한 하나의 거대 훅은 만들지 않는다.
- `Dialog`, `ConfirmDialog`를 제공해 Escape, 배경 클릭, 포커스 이동·복원, `aria` 속성을 통일한다.
- 기존 `confirm()`을 위험 작업용 공통 확인 대화상자로 순차 교체한다.
- 모달 작업 오류는 `ModalErrorAlert`를 계속 사용하고, 작업별 오류 상태를 분리한다.

**완료 조건:** 주요 모달이 키보드로 조작 가능하며, 목록 조회 실패가 빈 데이터 상태로 잘못 표시되지 않고, 공통 도메인 타입을 사용하는 화면 간 필드 정의가 일치한다.

### 단계 6. 통합 화면과 사용성 고도화

**목적:** 다수 사업을 빠르게 판단하고 상세 업무로 이동하게 한다.

- 통합 화면에서 사업, 카테고리, 태그, 상태 필터를 서버 기준으로 적용한다.
- 아카이브와 회의록을 사업 구분과 공개 권한을 유지한 채 한 화면에서 탐색하게 한다.
- 최초 조회와 더 보기 요청 모두 `response.ok`를 확인하고 오류·재시도 상태를 제공한다.
- 모바일 360px, 390px과 데스크톱 1440px에서 하단 탭, 필터, 긴 제목, 모달을 시각 검증한다.
- 접근성 점검에서 탭, 필터, 알림, 대화상자에 적절한 이름과 상태 전달을 보장한다.

**완료 조건:** 필터 오류나 API 오류가 빈 결과처럼 보이지 않으며, 모바일과 데스크톱에서 모든 주요 제어가 겹치지 않고 접근 가능하다.

### 단계 7. 품질 보증과 운영 준비

**목적:** 기능 배포 전 권한·데이터·실패 경로를 반복 검증한다.

- API 단위 테스트: 권한 매트릭스, 입력 검증, 오류 코드, 공개 범위, 초대 만료, 중복 요청.
- UI 테스트: 로딩, 빈 상태, 오류, 재시도, undo, 저장 실패 후 입력값 보존.
- 통합 테스트: 사업 생성부터 초대 수락, 카드·일정·문서 작성, 외부 문서 읽기까지의 흐름.
- 배포 전 `npx tsc --noEmit`, `npm run lint`, `npm run build`를 실행한다.
- 배포 후 핵심 API 상태, DB 오류, 5xx 비율, 외부 공유 실패를 모니터링한다.

**완료 조건:** 핵심 권한 시나리오와 실패 시나리오가 자동화되어 있고, 빌드·타입·린트 및 수동 반응형 점검을 통과한다.

## 6. 구현 순서와 의존성

| 순서 | 작업 | 선행 조건 | 결과 |
| --- | --- | --- | --- |
| 1 | 외부 DB 운영 점검 | 환경 변수와 외부 DB | 안전한 데이터 기반 |
| 2 | 요청 재시도 정책 | 공통 클라이언트 요청 모듈 | 중복 변경 방지 |
| 3 | 칸반 실패 복구 | 2 | 화면·서버 상태 일치 |
| 4 | 권한 가드·오류 계약 | 1 | API 일관성 |
| 5 | 초대·공유·기수 스키마 | 4 | 사업 참여 수명주기 |
| 6 | 아카이브 공개 범위·회의록 | 4, 5 | 문서 협업 완성 |
| 7 | 타입·다이얼로그·상태 UI | 2, 4 | 유지보수성과 접근성 향상 |
| 8 | 통합 화면 고도화 | 5, 6, 7 | 전 사업 탐색 경험 완성 |
| 9 | 자동화·운영 검증 | 1~8 | 배포 가능한 품질 |

## 7. 변경 관리 방식

- 한 번에 모든 API를 바꾸지 않고 도메인 묶음 단위로 이전한다.
- 표준 응답 도입 시 기존 클라이언트가 읽는 필드를 먼저 호환하거나 같은 변경에서 함께 전환한다.
- Prisma 변경은 스키마, 마이그레이션, 기존 데이터 보정, API 검증, UI 반영을 하나의 작업 단위로 완료한다.
- 각 단계 시작 전 현재 동작을 확인하는 회귀 목록을 만들고, 완료 후 같은 목록을 다시 실행한다.
- 관련 없는 디자인 변경과 리팩토링을 한 변경 묶음에 섞지 않는다.

## 8. 핵심 위험과 대응

| 위험 | 영향 | 대응 |
| --- | --- | --- |
| 변경 요청 자동 재시도 | 중복 사업·멤버·문서 생성 | 안전한 메서드만 기본 재시도, 멱등성 필요 시 서버 키 도입 |
| 낙관적 UI 실패 | 화면과 DB 상태 불일치 | 응답 확인, rollback 또는 재조회, 오류 표시 |
| 관리자 권한 불일치 | 과도하거나 누락된 접근 | 하나의 권한 가드와 권한 매트릭스 테스트 |
| 오류 형식 혼재 | 영문 오류 노출, 화면별 처리 차이 | 코드 기반 표준 오류 계약 |
| 외부 DB 전환 중 데이터 누락 | 운영 데이터 손실 | 이전 전 백업, 행 수·관계·권한 데이터 검증 |
| 공개 범위 설정 오류 | 내부 문서 노출 | 전용 공개 API, 서버 측 권한 검사, 링크 폐기 테스트 |
| 대형 공통화 | 기존 흐름 회귀 | 작은 도메인 단위 이전과 단계별 검증 |

## 9. 전체 완료 기준

- 모든 운영 데이터가 외부 DB에 저장되며 로컬 저장소 의존이 없다.
- 모든 보호 API가 인증, 권한, 입력 검증, 표준 오류 계약을 사용한다.
- 생성·수정·삭제 실패가 사용자에게 명확히 보이고 성공처럼 보이지 않는다.
- 사업 공유, 초대, 공개 범위, 외부 링크의 권한 규칙이 API와 UI에서 동일하다.
- 통합 화면에서 권한이 있는 사업의 칸반·일정·아카이브·회의록을 필터로 탐색할 수 있다.
- 모바일과 데스크톱의 핵심 경로가 접근 가능하고 반응형 검증을 통과한다.
- 타입 검사, 린트, 프로덕션 빌드, 권한·실패·주요 사용자 흐름 테스트를 통과한다.