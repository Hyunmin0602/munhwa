# 통합 상태 매핑 및 고급 필터 구현 계획

## 1. 결정

통합 화면은 사업별 원래 열을 모두 나열하지 않고, 아래 **세 개의 공통 상태 칸반**으로 제공한다.

```text
진행 전 → 진행 중 → 진행 완료
```

각 사업은 생성 또는 칸반 설정 시 자신의 실제 열을 세 공통 상태에 매핑한다. 통합 화면은 매핑된 상태를 기준으로 모든 사업의 카드를 한 보드에 모아 보여 주고, 카드에는 사업명과 원래 열 이름을 함께 표시한다.

이 방식은 전체 업무 현황을 빠르게 파악하는 통합 화면과, 사업마다 다른 세부 프로세스를 유지하는 사업별 칸반을 모두 만족시킨다.

## 2. 사용성 원칙

1. 통합 화면에서는 사용자가 세 상태만 이해하면 된다.
2. 사업별 칸반에서는 기존처럼 자유로운 실제 열 이름과 순서를 유지한다.
3. 카드를 통합 상태 칸으로 이동하면, 해당 사업에서 그 상태의 대표 열로 자동 이동한다.
4. 대표 열이 없는 상태로는 드래그할 수 없으며, 사용자에게 설정 필요 이유를 표시한다.
5. 설정하지 않은 실제 열의 카드는 통합 칸반에서 숨긴다. 데이터는 삭제되지 않으며 사업별 칸반에서 계속 보인다.
6. 새 사업의 기본 열 `할 일`, `진행 중`, `완료`는 각각 `진행 전`, `진행 중`, `진행 완료`로 자동 매핑한다.

## 3. 상태 매핑 모델

### 3.1 공통 상태값

| 코드 | 표시명 | 의미 |
| --- | --- | --- |
| `BEFORE` | 진행 전 | 시작 전, 준비, 기획, 요청, 백로그 |
| `IN_PROGRESS` | 진행 중 | 실행, 제작, 처리, 협의, 검토 중 |
| `DONE` | 진행 완료 | 완료, 종료, 결과 정리 |

### 3.2 데이터베이스 변경

`KanbanColumn`에 다음 필드를 추가한다.

| 필드 | 타입 | 목적 |
| --- | --- | --- |
| `integratedStatus` | `String?` | `BEFORE`, `IN_PROGRESS`, `DONE` 중 하나 또는 미설정 |
| `isIntegratedPrimary` | `Boolean` | 해당 상태에서 통합 화면의 카드 이동 도착 열인지 표시 |

권장 제약:

- 같은 사업에서 같은 `integratedStatus`를 여러 실제 열에 붙일 수 있다.
- 다만 `isIntegratedPrimary = true`인 열은 상태별로 사업당 최대 하나만 허용한다.
- SQLite의 부분 고유 인덱스 제약 관리 난이도를 고려해, 최초 구현에서는 API 트랜잭션 검증으로 단일 대표 열을 보장한다.
- 이후 데이터베이스가 PostgreSQL 등 부분 인덱스를 안정적으로 지원하는 환경으로 변경되면 DB 제약을 추가한다.

예시:

| 사업 | 실제 열 | 통합 상태 | 대표 열 |
| --- | --- | --- | --- |
| 문화제 | 기획 | `BEFORE` | 예 |
| 문화제 | 섭외 | `BEFORE` | 아니오 |
| 문화제 | 현장 운영 | `IN_PROGRESS` | 예 |
| 문화제 | 결과 보고 | `DONE` | 예 |
| 겨울 MT | 장소 대관 | `BEFORE` | 예 |
| 겨울 MT | 참가자 모집 | `IN_PROGRESS` | 예 |
| 겨울 MT | 종료 | `DONE` | 예 |

`기획`과 `섭외` 카드 모두 통합 화면의 `진행 전` 열에 보인다. 반면 통합 화면에서 어떤 카드를 `진행 전`으로 되돌리면 문화제 사업의 대표 열인 `기획`으로 이동한다.

## 4. 사업 생성·설정 흐름

### 4.1 새 사업 생성

새 사업 모달에 다음을 포함한다.

1. 사업명 — 필수
2. 사업 소개 — 선택, 최대 5,000자
3. 카테고리 — 선택, 최대 50자
4. 태그 — 선택, 최대 10개·각 30자
5. 색상
6. 기본 워크플로 선택

기본 워크플로는 아래 둘 중 하나다.

| 선택 | 생성 열 | 매핑 |
| --- | --- | --- |
| 기본 3단계 — 기본값 | 할 일 / 진행 중 / 완료 | 세 상태에 각각 대표 열 자동 설정 |
| 사용자 정의 | 사용자가 열 이름을 입력 | 생성 직후 매핑 설정 화면으로 이동 |

기존에 기본 열을 생성하는 API와 호환성을 유지하며, 기본 3단계 선택 시 서버가 매핑값까지 함께 생성한다.

### 4.2 사업별 칸반 설정

기존 사업별 칸반에 `통합 상태 설정` 진입 버튼을 둔다. 프로젝트 소유자 및 콘텐츠 관리 권한자만 변경할 수 있다.

설정 화면의 각 실제 열은 아래를 선택한다.

```text
[열 이름: 섭외]
통합 화면 상태:  (미표시) / 진행 전 / 진행 중 / 진행 완료
대표 이동 열:    [ ]
```

규칙:

- `미표시` 열은 통합 칸반에 나타나지 않는다.
- 특정 상태에 처음 매핑하는 열은 자동으로 대표 열이 된다.
- 대표 열을 다른 열로 바꾸면 기존 대표 열의 체크는 자동 해제된다.
- 대표 열을 해제하려 할 때 해당 상태에 다른 대표 열이 없다면, 먼저 다른 열을 선택하도록 안내한다.
- 삭제하려는 열이 대표 열이고 카드가 존재한다면, 삭제 전에 대체 대표 열을 지정하도록 요구한다.

## 5. 통합 칸반 화면

```text
통합 칸반
[검색: 사업명, 소개, 태그, 카드 제목] [필터] [초기화] [카드 추가]

적용 필터: [카테고리: 행사 ×] [태그: 2026 ×] [마감일: 이번 주 ×]

┌ 진행 전 ─ 12 ┐      ┌ 진행 중 ─ 8 ┐      ┌ 진행 완료 ─ 15 ┐
│ [문화제] 연사 섭외 │  │ [MT] 장소 답사     │  │ [문화제] 예산 집행 │
│ 원래 열: 섭외      │  │ 원래 열: 현장 운영 │  │ 원래 열: 결과 보고 │
└───────────────────┘  └───────────────────┘  └────────────────────┘
```

### 5.1 카드 표시

각 카드는 다음을 표시한다.

- 사업 색상, 사업명
- 실제 열 이름: `원래 열: 섭외`
- 작업 제목
- 우선순위, 담당자, 마감일

원래 열 이름은 사용자가 카드가 어떤 세부 프로세스에 있었는지 이해하도록 돕는다.

### 5.2 카드 이동

- 같은 공통 상태 안에서 카드 순서를 조정할 수 있다.
- 다른 공통 상태에 카드가 드롭되면 해당 사업의 대표 열로 이동한다.
- 대상 상태에 대표 열이 없으면 드롭을 취소하고 `이 사업의 '진행 중' 대표 열을 먼저 설정하세요.`를 표시한다.
- 서로 다른 사업의 카드를 섞어도 카드의 소속 사업 자체는 변경되지 않는다.
- 통합 화면의 드래그는 실제 열 구조를 변경하지 않고 카드의 `columnId`만 해당 사업 내부에서 변경한다.

### 5.3 미매핑 카드 안내

통합 화면 상단에 `통합 화면에 표시되지 않는 카드 n개` 안내를 제공한다.

- 클릭하면 사업별 매핑 설정이 필요한 열 목록을 보여 준다.
- 일반 멤버에게는 사업별 칸반 이동 링크만 제공한다.
- 설정 권한자에게만 `통합 상태 설정` 버튼을 제공한다.

## 6. 필터링 설계

### 6.1 검색 범위

검색어는 250~300ms 디바운스 후 아래 필드를 대상으로 한다.

- 사업명
- 사업 소개
- 카테고리
- 태그
- 작업 제목
- 실제 열 이름

작업 설명 전문 검색은 데이터 규모와 검색 품질을 확인한 뒤 추가한다.

### 6.2 필터 항목

| 필터 | 다중 선택 | 규칙 |
| --- | --- | --- |
| 사업명 | 가능 | 선택한 사업 중 하나 |
| 카테고리 | 가능 | 선택한 카테고리 중 하나 |
| 태그 | 가능 | 기본 OR, 고급 옵션에서 AND |
| 사업 상태 | 가능 | 선택한 상태 중 하나 |
| 통합 상태 | 가능 | 진행 전/중/완료 중 하나 |
| 실제 열 | 가능 | 선택한 실제 열 중 하나 |
| 담당자 | 가능 | 선택한 사용자 중 하나 |
| 우선순위 | 가능 | 낮음/보통/높음 중 하나 |
| 마감일 | 단일 | 전체/오늘/이번 주/기한 지남/미지정 |
| 빈 열 숨기기 | 단일 | 카드가 없는 통합 상태 열을 숨김 |

서로 다른 필터 그룹은 AND로 결합한다.

예시: `카테고리=행사` AND `태그=2026 또는 대외협력` AND `통합 상태=진행 중` AND `마감일=이번 주`

### 6.3 UI 규칙

- 데스크톱: 필터 버튼에서 우측 패널 또는 팝오버를 연다.
- 모바일: 하단 시트로 연다.
- 필터 패널의 선택은 임시 상태다. `적용` 전에는 보드 및 URL을 바꾸지 않는다.
- 적용된 필터는 검색창 아래 칩으로 보이며, `×`로 개별 해제한다.
- `초기화`는 검색어 및 전체 필터를 제거한다.
- 상태는 URL 쿼리로 유지한다.

```text
?q=문화제
&projectIds=p1,p2
&categories=행사,교육
&tags=2026,대외협력
&tagMode=any
&integratedStatuses=BEFORE,IN_PROGRESS
&columnIds=c1,c2
&assigneeIds=u1
&priorities=high
&due=week
&hideEmpty=true
```

## 7. API 및 권한

### 7.1 통합 조회 API

다음 읽기 API를 추가하거나 기존 통합 API에 `view=kanban` 모드를 추가한다.

```text
GET /api/dashboard/integrated/kanban
```

응답에는 필터링된 카드와 함께 사업, 실제 열, 통합 상태, 필터 선택지를 포함한다.

```ts
{
  projects: Array<{
    id: string;
    name: string;
    description: string | null;
    category: string | null;
    tags: string[];
    color: string;
    status: string;
  }>;
  columns: Array<{
    id: string;
    projectId: string;
    name: string;
    order: number;
    integratedStatus: "BEFORE" | "IN_PROGRESS" | "DONE" | null;
    isIntegratedPrimary: boolean;
    tasks: Task[];
  }>;
  filterOptions: {
    categories: string[];
    tags: Array<{ value: string; count: number }>;
    members: Array<{ id: string; name: string | null }>;
    statuses: string[];
  };
  summary: {
    mappedTaskCount: number;
    unmappedTaskCount: number;
  };
}
```

서버는 항상 다음 순서로 실행한다.

1. 인증 사용자의 접근 가능한 사업을 결정한다.
2. 모든 사업·카테고리·태그·검색 조건을 그 접근 범위 안에서만 적용한다.
3. 매핑된 열과 카드만 반환한다.
4. 필터 옵션도 접근 가능한 사업만 기준으로 생성한다.

### 7.2 매핑 수정 API

```text
PATCH /api/projects/:projectId/columns/:columnId
```

기존 열 수정 API에 아래 검증된 필드를 추가한다.

```ts
{
  integratedStatus?: "BEFORE" | "IN_PROGRESS" | "DONE" | null;
  isIntegratedPrimary?: boolean;
}
```

- 프로젝트 소유자 또는 콘텐츠 관리 권한자만 상태 매핑을 변경할 수 있다.
- 대표 열 지정은 트랜잭션으로 처리한다.
- 같은 사업·상태에 기존 대표 열이 있으면 자동 해제한 뒤 새 열을 대표로 만든다.
- 상태 매핑 값과 불리언 값은 서버에서 엄격히 검증한다.

### 7.3 작업 쓰기 API

기존 API를 계속 사용한다.

- `POST /api/projects/:projectId/tasks`
- `PATCH /api/projects/:projectId/tasks/:taskId`
- `DELETE /api/projects/:projectId/tasks/:taskId`

통합 화면의 카드 이동은 해당 사업의 대표 열 ID만 전송한다. 서버의 사업 멤버십과 대상 열 소유권 검증은 유지한다.

## 8. 구현 단계

| 단계 | 작업 | 완료 기준 |
| --- | --- | --- |
| 1 | Prisma 마이그레이션: 열의 통합 상태·대표 열 필드 추가 | 기존 열은 모두 미매핑, 데이터 손실 없음 |
| 2 | 새 사업 기본 3열에 자동 매핑 추가 | 새 사업은 즉시 통합 칸반에서 정상 표시 |
| 3 | 사업 생성·수정 폼에 사업 소개 추가 | `description` 입력·수정 가능 |
| 4 | 태그를 정규화한 칩 입력 UI로 개선 | 빈 값·중복 태그 제거, 최대 10개 제한 |
| 5 | 사업별 `통합 상태 설정` UI와 매핑 API 구현 | 대표 열 단일성·권한·검증 보장 |
| 6 | 통합 칸반 조회 API 구현 | 접근 가능한 사업의 매핑 카드와 필터 옵션 반환 |
| 7 | 기존 자동 이름 분류 제거, 3개 공통 열 UI 구현 | 실제 열 대신 명시적 상태 매핑으로 카드 표시 |
| 8 | 카드 이동·생성 흐름 구현 | 사업별 대표 열을 통해 안전하게 저장 |
| 9 | 검색, 카테고리, 태그, 상태, 담당자, 우선순위, 마감일 필터 구현 | 필터 조합·URL 동기화·필터 칩 정상 동작 |
| 10 | 미매핑 현황, 빈 상태, 오류, 모바일 필터 시트 구현 | 설정 누락이 숨겨진 데이터로 오해되지 않음 |
| 11 | 권한·성능·반응형 회귀 검증 | 아래 검증 기준 통과 |

## 9. 검증 기준

### 기능

- 사업마다 다른 실제 열이 세 통합 상태 중 지정된 상태에 표시된다.
- 매핑되지 않은 열의 카드는 통합 화면에 표시되지 않고, 미매핑 안내 수에 반영된다.
- 한 사업의 여러 열이 같은 통합 상태에 있더라도 원래 열 이름을 카드에서 식별할 수 있다.
- 카드를 다른 상태로 옮기면 같은 사업의 해당 상태 대표 열로 저장된다.
- 대표 열이 없으면 이동이 저장되지 않으며 안내가 보인다.
- 사업 소개, 카테고리, 태그, 사업명, 작업 제목, 실제 열 이름 검색이 동작한다.
- 모든 필터가 예상한 AND/OR 규칙으로 결합되고 URL에 유지된다.

### 보안

- 일반 사용자는 비소속 사업의 열·카드·카테고리·태그를 API에서 받을 수 없다.
- `admin`과 유효한 `space_admin`은 현행 정책에 따라 전체 사업을 조회·관리한다.
- 대표 열 설정과 카드 이동은 프로젝트 권한 및 열 소유권 검증을 통과해야 한다.
- 필터 배열, 검색어, 상태값은 길이·개수·허용값을 서버에서 검증한다.

### 반응형 및 성능

- 360px에서는 세 상태 열을 가로 스크롤하고 필터는 하단 시트로 제공한다.
- 768px와 1440px에서 필터, 열 헤더, 카드 메타데이터가 겹치지 않는다.
- 서버 필터를 우선 적용하고, 카드 300개 또는 열 30개 이상에서는 점진 로딩/가상화를 추가 검토한다.

## 10. 비범위

- 사업 간 카드 이동
- 통합 화면에서 실제 열 생성·이름 변경·삭제·순서 변경
- 태그 권한, 태그 관리자, 공용 태그 사전
- 문서·회의록 전문 검색

이 기능들은 사용 패턴과 운영 규칙이 확정된 뒤 별도로 검토한다.
