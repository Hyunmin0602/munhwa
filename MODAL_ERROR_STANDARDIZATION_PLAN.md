# 모달 오류 표시 표준화 및 통일 적용 계획

작성일: 2026-09-19

## 1. 목적

모달에서 발생한 요청 실패를 모달 내부에서 즉시 확인하고 다음 행동을 알 수 있게 한다. 페이지 뒤쪽 경고, 무음 실패, API 원문 오류 노출을 없애며 모든 모달에 같은 표현과 접근성 기준을 적용한다.

## 2. 현재 불일치

| 화면 | 현재 상태 | 사용자 문제 |
| --- | --- | --- |
| 사업 생성 | 빨간 일반 텍스트 | 오류 유형과 재시도 행동이 일관되지 않음 |
| 사업 수정·멤버 관리 | 하나의 일반 오류 상태를 여러 작업이 공유 | 어떤 작업이 실패했는지 맥락이 흐려짐 |
| 일정 추가 | 오류가 모달 뒤 페이지에 표시될 수 있음 | 저장 실패를 모달 안에서 확인할 수 없음 |
| 카드 상세 | 저장 실패가 무음 처리됨 | 저장·닫기 후 모달이 남아도 이유를 알 수 없음 |
| 이메일 찾기 | 별도 스타일의 오류 문구 | 다른 모달과 시각·접근성 기준이 다름 |

## 3. 표준

### 표시 위치와 형태

- 오류는 오류를 발생시킨 모달의 제목 아래 또는 해당 작업 영역 바로 위에 표시한다.
- 공통 `ModalErrorAlert`를 사용한다.
- 경고는 `role="alert"`, `aria-live="assertive"`를 갖고 장식 아이콘과 문구를 함께 표시한다.
- 오류가 있는 동안 모달은 닫히지 않으며 입력 값은 유지한다.
- 재시도가 같은 요청을 다시 보낼 수 있는 경우 `다시 시도` 버튼을 제공한다.

### 상황별 문구

| 상황 | 표준 문구 |
| --- | --- |
| 입력 검증 | `입력 내용을 확인한 뒤 다시 시도해주세요.` |
| 권한 없음 | `이 작업을 수행할 권한이 없습니다.` |
| 대상 없음 | `대상을 찾을 수 없습니다. 목록을 새로고침한 뒤 다시 시도해주세요.` |
| 세션 만료 | 공통 인증 처리로 로그인 화면 이동 |
| 네트워크 실패 | `요청을 완료하지 못했습니다. 네트워크 연결을 확인한 뒤 다시 시도해주세요.` |
| 서버 처리 실패 | `{작업명}을 완료하지 못했습니다. 잠시 후 다시 시도해주세요.` |

서버의 구체적인 검증 오류가 한국어 사용자 문구일 경우에만 입력 검증 메시지로 표시한다. 내부 오류 코드나 영문 API 메시지는 직접 노출하지 않는다.

## 4. 적용 순서

1. 공통 오류 알림 컴포넌트와 HTTP 상태별 메시지 변환 함수를 추가한다.
2. 사업 생성·수정 모달을 공통 오류 컴포넌트와 `apiFetch`로 통일한다.
3. 일정 추가 모달의 오류 상태를 페이지 오류와 분리해 모달 내부에 표시한다.
4. 카드 상세 모달에 저장 오류와 재시도 동작을 추가한다.
5. 이메일 찾기 모달을 같은 컴포넌트로 교체한다.
6. 아카이브 편집기는 모달이 아니므로 별도 페이지 오류 표준화 작업으로 분리한다.

## 5. 재검토 기준

- `400`, `403`, `404`, 네트워크 예외, `500`에서 사용자 문구가 표준과 일치한다.
- 저장 실패 시 입력 값과 모달 열림 상태가 유지된다.
- 일정 추가 실패가 오버레이 뒤에 가려지지 않는다.
- 오류 경고가 화면 판독기에 즉시 전달된다.
- 기존 API의 세션 만료 리디렉션을 방해하지 않는다.

## 6. 검증 계획

각 대상 모달에서 잘못된 입력, 권한 없는 요청, 네트워크 차단, API 500 응답을 모의해 오류 위치·문구·재시도와 입력값 보존을 확인한다. 구현 후 `npx tsc --noEmit`, `npm run build`를 실행한다.

## 7. 적용 및 재검토 결과

- 공통 `ModalErrorAlert`를 추가하고 사업 생성·수정, 일정 추가, 카드 상세, 이메일 찾기에 적용했다.
- 사업 수정은 원시 `fetch` 대신 공통 `apiFetch`를 사용하도록 맞춰 세션 만료 처리도 통일했다.
- 일정 추가 오류는 페이지 뒤쪽 경고가 아닌 모달 내부에 표시하도록 분리했다.
- 재검토에서 발견한 합성 폼 이벤트 재시도는 제거하고 저장 함수를 직접 재호출하도록 정리했다.
- 아카이브 편집기는 페이지형 화면이므로 이 계획의 모달 범위에서는 제외하며, 별도의 페이지 오류 표준화 대상으로 유지한다.